---
phase: 03-manager-skeleton-state-leases-api-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/manager/package.json
  - packages/manager/tsconfig.json
  - packages/manager/vitest.config.ts
  - packages/manager/src/index.ts
  - packages/manager/test/smoke.test.ts
  - packages/manager/test/helpers/platform.ts
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/vitest.config.ts
  - packages/cli/src/index.ts
  - packages/cli/test/smoke.test.ts
  - .github/workflows/ci.yml
  - test/ci-matrix.test.ts
  - pnpm-lock.yaml
autonomous: false
requirements: [EXEC-01, EXEC-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pnpm --filter @adl/manager test` and `pnpm --filter @adl/cli test` both exit 0 against a real (non-placeholder) package."
    - "`.github/workflows/ci.yml` runs the suite on a `windows-latest` runner as well as `ubuntu-latest`, and the Linux-only worker-user provisioning step does not execute on the Windows leg."
    - "A platform-gated assertion in `@adl/manager` writes a stated reason to stderr when it skips, and a run on the platform where it applies with the gate misconfigured fails rather than skipping."
  artifacts:
    - packages/manager/package.json
    - packages/manager/vitest.config.ts
    - packages/manager/src/index.ts
    - packages/manager/test/helpers/platform.ts
    - packages/cli/package.json
    - packages/cli/vitest.config.ts
    - packages/cli/src/index.ts
    - test/ci-matrix.test.ts
  key_links:
    - "Root `vitest.config.ts` globs `packages/*/vitest.config.ts`, so the two new packages enrol in `pnpm test` with no edit to the root config."
    - "`@adl/cli`'s `package.json` lists neither `@adl/db` nor `@adl/manager`, so pnpm's strict `node_modules` makes a reach past HTTP a resolve-time failure (D-18, D-21)."
    - "`.github/workflows/ci.yml`'s cache key already interpolates `runner.os`, so adding an `os` matrix dimension does not cross-contaminate the pnpm store cache."
---

<objective>
Stand up the Wave-0 infrastructure the rest of Phase 3 cannot be written without: the two new
packages (`@adl/manager`, `@adl/cli`), the cross-platform CI matrix leg D-33 requires, and the
visible-skip test gate that keeps a platform-gated recovery assertion from passing vacuously.

Purpose: `03-VALIDATION.md` records that **every** test file in this phase is new because
`@adl/manager` and `@adl/cli` do not exist, and that `.github/workflows/ci.yml` currently runs
`ubuntu-latest` only. Both are named as required Wave 0 work, not follow-ups.
Output: two installable, testable packages; a three-leg CI matrix; a greppable skip discipline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-manager-skeleton-state-leases-api-cli/03-CONTEXT.md
@.planning/phases/03-manager-skeleton-state-leases-api-cli/03-PATTERNS.md
@.planning/phases/03-manager-skeleton-state-leases-api-cli/03-VALIDATION.md
@packages/workspace/package.json
@packages/workspace/tsconfig.json
@packages/workspace/vitest.config.ts
@packages/workspace/test/helpers/platform.ts
@.github/workflows/ci.yml
</context>

<phase_goal_note>
**MVP mode is on, but no user story exists for this phase.** The ROADMAP `**Goal:**` line for
Phase 3 reads: *"A crash-surviving control plane the maintainer can watch and interrupt, proven
with a fake worker and no AI anywhere in the loop."* That is an outcome statement, not an
`As a / I want to / so that` user story, so none has been invented here. Run `/gsd mvp-phase 3`
if a story is wanted; the plans proceed on the Goal as written.
</phase_goal_note>

<edge_probe_ledger>
## Edge-probe accounting (phase-level ledger — 13 rows, 0 silent drops)

This phase has no SPEC.md, so `## Edge Coverage` was produced by the spec-less probe fallback.
13 applicable edges were surfaced. **13 == 6 authored into `must_haves.truths` + 7 surfaced as
flagged assumptions.** Nothing was dropped and nothing was auto-dismissed.

| # | Requirement | Category | Disposition | Lands in |
|---|-------------|----------|-------------|----------|
| 1 | EXEC-05 | adjacency | authored — `verification: explicit` | `03-07` truths |
| 2 | EXEC-05 | empty | authored — `verification: explicit` | `03-07` truths |
| 3 | EXEC-05 | ordering | authored — `verification: explicit` | `03-07` truths |
| 4 | OBS-01 | adjacency | authored — `verification: explicit` | `03-08` truths |
| 5 | OBS-01 | empty | authored — `verification: explicit` | `03-08` truths |
| 6 | OBS-01 | ordering | authored — `verification: explicit` | `03-08` truths |
| 7 | EXEC-01 | unclassified | **flagged assumption — unresolved** | `03-06` |
| 8 | EXEC-02 | unclassified | **flagged assumption — unresolved** | `03-04` |
| 9 | EXEC-03 | unclassified | **flagged assumption — unresolved** | `03-05` |
| 10 | EXEC-04 | unclassified | **flagged assumption — unresolved** | `03-05` |
| 11 | EXEC-06 | unclassified | **flagged assumption — unresolved** | `03-06` |
| 12 | OBS-03 | unclassified | **flagged assumption — unresolved** | `03-07` |
| 13 | OBS-04 | unclassified | **flagged assumption — unresolved** | `03-07` |

An `unclassified` row is never auto-resolved with a backstop marker. Each of the seven is written
out as an explicit `<flagged_assumptions>` entry in the plan named above, where a human reviewing
that plan will see it.
</edge_probe_ledger>

<prohibition_ledger>
## Prohibition ledger (§B recall — 4 kept, descriptor-less)

Stage 1 over-produced ~11 candidates across the nine requirements; Stage 2 dropped the
routine-engineering ones. Four bespoke values/safety prohibitions survive, each authored
descriptor-less (no `check_*` scalar) into the `must_haves.prohibitions` block of its owning plan:

| # | Category | Owning plan | Substance |
|---|----------|-------------|-----------|
| P1 | safety | `03-06` | Never signal a process ADL cannot positively attribute to a worker it spawned |
| P2 | safety | `03-06` | Never migrate or write an adopter's database without a recoverable pre-migration copy |
| P3 | transparency | `03-09` | Never report a platform-gated recovery assertion as passing when it did not execute |
| P4 | transparency | `03-08` | Never present state the manager did not observe as though it had |

**Canon-referral breadcrumbs (recalled, deliberately NOT minted):**
- Constant-time bearer-token comparison and secrets-in-logs are OWASP canon — covered by
  `/gsd-secure-phase` and the `<threat_model>` blocks in `03-04` and `03-06`; not minted here.
- Non-loopback default bind is OWASP secure-defaults canon — covered by D-19 and `T-3-01`; not
  minted here.
- Schema-validating untrusted HTTP and IPC input is canon input validation — covered by `T-3-07`;
  not minted here.
</prohibition_ledger>

<artifacts_produced>
## Artifacts this phase produces (plan 03-01 subset)

**New packages:** `@adl/manager`, `@adl/cli`

**New files:**
- `packages/manager/package.json`, `tsconfig.json`, `vitest.config.ts`, `src/index.ts`, `test/smoke.test.ts`
- `packages/manager/test/helpers/platform.ts` — exports `windowsOnly`, `requirePlatform`, `PlatformGate`, `SKIP_PREFIX`
- `packages/cli/package.json`, `tsconfig.json`, `vitest.config.ts`, `src/index.ts`, `test/smoke.test.ts`
- `test/ci-matrix.test.ts`

**New package manifests / fields:** `@adl/cli`'s `bin` field mapping `adl` to `./dist/index.js`
(the one field the `@adl/workspace` template does not have).

**New CI surface:** `.github/workflows/ci.yml` matrix key `os`, values `ubuntu-latest` and
`windows-latest`, with one `exclude` entry.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — hono and @hono/node-server</name>
  <read_first>
    - `.planning/phases/03-manager-skeleton-state-leases-api-cli/03-RESEARCH.md` § "Package Legitimacy Audit"
    - `.planning/phases/01-core-contracts/01-01-PLAN.md` (the Phase 1 precedent for this gate's shape)
  </read_first>
  <what-built>
    Nothing yet. This gate runs BEFORE the first `pnpm add` of this phase, per the package
    legitimacy protocol. `03-RESEARCH.md`'s audit table returned `[SUS]` with a single signal —
    `too-new` — for two packages, and `too-new` measures the most recent publish date rather than
    the package's age or trustworthiness.
  </what-built>
  <how-to-verify>
    Open each of the two pages below and confirm the four facts listed under it. This is expected
    to be fast: the research pass assessed both flags as false positives (49.3M and 45.8M weekly
    downloads, established `honojs` GitHub organisation, no postinstall script, and both versions
    already named with HIGH confidence in `./.claude/CLAUDE.md`'s own stack research). The check is
    procedurally binding regardless of that assessment.

    1. https://www.npmjs.com/package/hono
       - The `Repository` link points at `github.com/honojs/hono`.
       - Version `4.13.2` is listed under `Versions` and is not deprecated.
       - Weekly downloads are in the tens of millions, not the hundreds.
       - The publish date of the newest version is recent — this is the `too-new` signal itself;
         confirm it looks like a routine version bump, not a first-ever publish.

    2. https://www.npmjs.com/package/@hono/node-server
       - The `Repository` link points at `github.com/honojs/node-server`.
       - Version `2.1.1` is listed and is not deprecated.
       - Weekly downloads are in the tens of millions.
       - The package is published under the `@hono` scope owned by the same organisation.

    Also confirm the three already-approved packages look unremarkable if you want the whole set in
    one pass: `commander@15.0.0`, `pino@10.3.1`, `croner@10.0.1`.
  </how-to-verify>
  <acceptance_criteria>
    - A human has stated, in the resume signal, that both npm pages resolve to the `honojs`
      organisation and that neither version is deprecated.
    - No `pnpm add` of `hono` or `@hono/node-server` has run before this checkpoint resolves.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the installs, or name the package and what looked wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold @adl/manager and @adl/cli as real packages</name>
  <files>packages/manager/package.json, packages/manager/tsconfig.json, packages/manager/vitest.config.ts, packages/manager/src/index.ts, packages/manager/test/smoke.test.ts, packages/cli/package.json, packages/cli/tsconfig.json, packages/cli/vitest.config.ts, packages/cli/src/index.ts, packages/cli/test/smoke.test.ts, pnpm-lock.yaml</files>
  <read_first>
    - `packages/workspace/package.json` — the template; the most recently created package and therefore the current shape
    - `packages/workspace/tsconfig.json` — copy verbatim, adjusting only the leading comment
    - `packages/workspace/vitest.config.ts` — copy, changing only `name`
    - `packages/workspace/src/index.ts` — the barrel header-comment style, and the "why public" comment convention on each export group
    - `pnpm-workspace.yaml` — the `catalog:` entries for `typescript`, `zod`, `vitest`
    - `vitest.config.ts` (root) — confirms `packages/*/vitest.config.ts` auto-enrolment, so no root edit is needed
    - `.planning/phases/03-manager-skeleton-state-leases-api-cli/03-PATTERNS.md` § "packages/manager/package.json, tsconfig.json, vitest.config.ts"
  </read_first>
  <action>
    Create `packages/manager` and `packages/cli` as real, installable packages — no placeholders,
    per Phase 1 D-25.

    `packages/manager/package.json`: name `@adl/manager`, `"type": "module"`, `version` `0.0.0`,
    `license` `MIT`, `main`/`types`/`files`/`exports` exactly as `@adl/workspace` has them, and the
    same five `scripts` keys (`build`, `pretypecheck`, `typecheck`, `pretest`, `test`). The
    `description` states its dependency scope: the manager daemon — lease queue, worker supervision,
    HTTP API. `dependencies`: `@adl/core` at `workspace:*`, `@adl/db` at `workspace:*` (a real
    dependency, not a devDependency — the manager is the only writer), `@adl/workspace` at
    `workspace:*`, `hono` at `4.13.2`, `@hono/node-server` at `2.1.1`, `pino` at `10.3.1`, `croner`
    at `10.0.1`, `ulid` at `3.0.2`, and `zod` at `catalog:`. `devDependencies`: `@types/node` at
    `22.20.1`, `typescript` at `catalog:`, `vitest` at `catalog:`. `pretypecheck` builds `../core`;
    `pretest` builds `../core ../db ../workspace`.

    `packages/cli/package.json`: name `@adl/cli`, same `type`/`license`/`main`/`types`/`exports`
    shape, plus the one field the template does not have — `"bin": { "adl": "./dist/index.js" }`,
    because this package ships the executable (D-21: two packages, one binary). `dependencies`:
    `commander` at `15.0.0` and `zod` at `catalog:`. It lists **neither** `@adl/db` nor
    `@adl/manager` and must not gain either: D-18 and D-21 make the CLI an HTTP client, and pnpm's
    strict `node_modules` turns a reach past HTTP into a resolve-time failure rather than a review
    finding. `files` includes `dist`. Its `pretest`/`pretypecheck` need nothing built.

    `tsconfig.json` for both: copy `packages/workspace/tsconfig.json` verbatim — `extends`
    `../../tsconfig.base.json`, `rootDir` `src`, `outDir` `./dist`, `tsBuildInfoFile`
    `./dist/.tsbuildinfo`, `types` `["node"]`, `include` `["src/**/*.ts"]`. Add a leading comment
    naming why `node` types are needed in each case: `child_process.fork` for the manager,
    `process.exit`/stdout for the CLI.

    `vitest.config.ts` for both: copy `packages/workspace/vitest.config.ts`, changing only the
    project `name` to `manager` and `cli` respectively. Do **not** edit the root `vitest.config.ts`
    — it globs `packages/*/vitest.config.ts` and its own comment states that adding a package
    enrols it automatically, which is what keeps concurrent plans off a shared file.

    `packages/manager/src/index.ts` and `packages/cli/src/index.ts`: barrels with the header-comment
    style `packages/workspace/src/index.ts` establishes — a docblock stating what the package is
    for and what boundary it holds, and a "why public" comment above each export group rather than
    a bare list. At this point each has nothing to export yet; state that in the header and leave the
    file with a single named export placeholder that is real (for the manager, a
    `MANAGER_PACKAGE_NAME` constant is acceptable only if it is consumed by the smoke test; prefer
    exporting nothing and having the smoke test assert the module loads).

    `packages/manager/test/smoke.test.ts` and `packages/cli/test/smoke.test.ts`: one test each that
    imports the package's own barrel and asserts it resolves. This is what makes the package
    testable rather than merely present, and it is what `pnpm --filter <pkg> test` exercises before
    any behaviour exists.

    Run `pnpm install` at the workspace root so `pnpm-lock.yaml` records the new packages and the
    five new third-party dependencies. Then run `pnpm -r typecheck` and `pnpm -r test` and confirm
    both new projects appear in the output.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false &amp;&amp; pnpm -r typecheck &amp;&amp; pnpm --filter @adl/manager test &amp;&amp; pnpm --filter @adl/cli test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/manager/package.json` declares `"hono": "4.13.2"`, `"@hono/node-server": "2.1.1"`, `"pino": "10.3.1"`, `"croner": "10.0.1"`, `"ulid": "3.0.2"`, and `"@adl/db": "workspace:*"` under `dependencies`.
    - `packages/cli/package.json` declares `"commander": "15.0.0"` and a `bin` entry mapping `adl` to `./dist/index.js`.
    - `node -e "const p=require('./packages/cli/package.json');const d={...p.dependencies,...p.devDependencies};process.exit(('@adl/db' in d)||('@adl/manager' in d)?1:0)"` exits 0.
    - `pnpm --filter @adl/manager test` exits 0 and its output names the project `manager`.
    - `pnpm --filter @adl/cli test` exits 0 and its output names the project `cli`.
    - `pnpm -r typecheck` exits 0.
    - `git diff --stat vitest.config.ts` is empty — the root Vitest config was not edited.
  </acceptance_criteria>
  <done>Both packages install, typecheck, and run a green suite; the CLI cannot resolve `@adl/db` or `@adl/manager`.</done>
</task>

<task type="auto">
  <name>Task 3: The windows-latest CI leg and the manager's visible-skip gate</name>
  <files>.github/workflows/ci.yml, packages/manager/test/helpers/platform.ts, packages/manager/test/helpers/platform.test.ts, test/ci-matrix.test.ts</files>
  <read_first>
    - `.github/workflows/ci.yml` — the whole file; the `strategy.matrix` block, the `Provision the unprivileged worker user` step, the two `Test` steps, and the cache key that already interpolates `runner.os`
    - `packages/workspace/test/helpers/platform.ts` — the full file; `SKIP_PREFIX`, `LinuxOnlyGate`, and the deliberate asymmetry where a misconfigured Linux run THROWS rather than returning a variant
    - `test/toolchain.test.ts` — the existing precedent for a root-suite test that asserts on repository configuration
    - `.planning/phases/03-manager-skeleton-state-leases-api-cli/03-CONTEXT.md` D-33
  </read_first>
  <action>
    Two halves of the same guarantee: the recovery suite must actually run on Windows, and an
    assertion that has no Windows subject must say so out loud rather than pass.

    **CI matrix.** Add an `os` dimension to `strategy.matrix` in `.github/workflows/ci.yml` with
    values `ubuntu-latest` and `windows-latest`, and change `runs-on: ubuntu-latest` to
    `runs-on: ${{ matrix.os }}`. Update the job `name` so both dimensions appear in the run list.
    Add one `exclude` entry removing `windows-latest` at `node-version: 24`, leaving three legs:
    ubuntu/22, ubuntu/24, windows/22. The rationale to record in a YAML comment is D-33's own: the
    development machine is Windows running Node 22.23.2, so the Windows leg mirrors local iteration;
    Node 24 on Windows is a stated, accepted gap that costs one matrix line to close later.

    Set `defaults.run.shell: bash` at the job level so the existing `run:` blocks keep their POSIX
    syntax on the Windows runner (GitHub's Windows images ship bash).

    Gate the Linux-only steps with `if: runner.os == 'Linux'`: the entire
    `Provision the unprivileged worker user` step, and the `Test` step whose command is
    `sg adl-worker -c "pnpm -r test"`. Add a sibling `Test` step with
    `if: runner.os != 'Linux'` running plain `pnpm -r test` — `sg` does not exist on Windows and the
    privilege-drop provisioning it re-establishes group membership for has no Windows subject
    (Phase 2 D-05). The `Test (workspace root suite)` step runs unconditionally on every leg.

    Leave the `Locate the pnpm store` / `Cache the pnpm store` steps as they are; the cache key
    already interpolates `runner.os`, so the two platforms do not share a store entry.

    **The manager's platform gate.** Create `packages/manager/test/helpers/platform.ts` following
    `packages/workspace/test/helpers/platform.ts`'s shape rather than importing it — it lives in a
    different package's `test/` directory and is not on any barrel. Re-declare `SKIP_PREFIX` with
    the same value `[ADL][SKIPPED]`, since that string is what a reader greps a green CI log for.
    Export a `PlatformGate` discriminated union of `{ kind: 'run' }` and
    `{ kind: 'skip'; reason: string }`, a `requirePlatform(platform, reason, requirementId)` function,
    and a `windowsOnly(reason, requirementId)` convenience wrapper. A skip writes
    `${SKIP_PREFIX}[${requirementId}] ${reason} (platform: ${process.platform})` to `process.stderr`
    before returning, exactly as `linuxOnly` does. Both take the current platform as a defaulted
    parameter so the helper's own unit test can drive it without spawning a second process.

    The reason string is supplied by the caller, never generated: `linuxOnly`'s docblock states why
    ("not Linux" is not a reason) and that discipline carries over unchanged.

    **The assertion that the CI change happened.** Create `test/ci-matrix.test.ts` in the root suite.
    It reads `.github/workflows/ci.yml` as text and asserts: the matrix names `windows-latest`; the
    matrix still names `ubuntu-latest`; the provisioning step carries a `runner.os == 'Linux'`
    condition; and there is a test invocation reachable on a non-Linux runner. Assert on parsed
    structure where practical rather than on raw substrings, following `test/toolchain.test.ts`'s
    approach of checking the configuration rather than trusting it. A workflow file is not otherwise
    exercised by any local command, so without this test the leg can be silently deleted later and
    every local signal stays green.
  </action>
  <verify>
    <automated>pnpm vitest run --project root -- ci-matrix &amp;&amp; pnpm --filter @adl/manager test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run --project root` exits 0 and `test/ci-matrix.test.ts` is among the files it executed.
    - `.github/workflows/ci.yml` parses as YAML and its `jobs.verify.strategy.matrix.os` array contains both `ubuntu-latest` and `windows-latest`.
    - `.github/workflows/ci.yml` sets `runs-on` to the matrix `os` value rather than a literal.
    - The `Provision the unprivileged worker user` step carries an `if:` expression naming `runner.os` and `Linux`.
    - A test step exists whose `if:` expression makes it run on a non-Linux runner.
    - `packages/manager/test/helpers/platform.ts` exports `SKIP_PREFIX`, `PlatformGate`, `requirePlatform`, and `windowsOnly`.
    - A unit test drives `requirePlatform` with a mismatched platform argument, asserts the returned kind is `skip`, and asserts the stderr line begins with `[ADL][SKIPPED]` and names the requirement id.
  </acceptance_criteria>
  <done>CI runs three legs including a Windows one; the Linux-only provisioning is gated; a platform skip in `@adl/manager` is greppable in a green log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine / CI runner | Third-party package code executes at install and at test time |
| CI workflow definition → CI runner | A workflow edit changes what evidence a green tick represents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-SC | Tampering | `pnpm add hono @hono/node-server pino croner ulid commander` | high | mitigate | Task 1's blocking human checkpoint verifies both `[SUS]` packages against npmjs.com before any install; `pnpm-workspace.yaml`'s `allowBuilds` allowlist already means none of the five may run an install script without being named there, and none is added to it |
| T-3-10 | Repudiation | `.github/workflows/ci.yml` Windows leg | medium | mitigate | `test/ci-matrix.test.ts` asserts the leg exists in the local suite, so deleting it turns a local run red instead of silently reducing what a green tick means |
| T-3-18 | Repudiation | `packages/manager/test/helpers/platform.ts` | medium | mitigate | A skip writes a stated reason to stderr under the greppable `[ADL][SKIPPED]` prefix; the helper's own unit test asserts the line is emitted, so a vacuous green is detectable by grep rather than by inference |
| T-3-19 | Elevation of Privilege | Linux worker-user provisioning on a non-Linux runner | low | accept | The provisioning step is gated off on Windows rather than made cross-platform; Phase 2 D-05 already names Linux as the deployment target with documented degradation elsewhere, and the step grants sudo only to run commands as one specific unprivileged user |
</threat_model>

<verification>
- `pnpm install` resolves with the two new packages present and five new third-party dependencies locked.
- `pnpm -r typecheck` exits 0 with `manager` and `cli` included.
- `pnpm test` (i.e. `pnpm -r test && vitest run --project root`) exits 0.
- `.github/workflows/ci.yml` parses and describes three legs.
- `pnpm lint` and `pnpm format` exit 0 — the new packages are covered by the existing flat config globs with no eslint edit in this plan.
</verification>

<success_criteria>
- `@adl/manager` and `@adl/cli` are real, installed, typechecked, tested packages — not placeholders.
- `@adl/cli` structurally cannot resolve `@adl/db` or `@adl/manager`.
- CI runs on `windows-latest` as well as `ubuntu-latest`, with Linux-only steps gated.
- A platform-gated assertion in `@adl/manager` skips loudly or fails; it never passes vacuously.
</success_criteria>

<output>
Create `.planning/phases/03-manager-skeleton-state-leases-api-cli/03-01-SUMMARY.md` when done
</output>
