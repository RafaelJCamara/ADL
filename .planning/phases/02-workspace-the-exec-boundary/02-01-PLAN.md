---
phase: 02-workspace-the-exec-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [WORK-01, WORK-02]

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A human has confirmed on the public registry page that `execa` and `simple-git` are the packages the research names, before either is added to any manifest."
    - "No package-manager install of a workspace runtime dependency happens in this phase until this gate is resolved."
  artifacts: []
  key_links: []
  prohibitions:
    - statement: "MUST NOT auto-approve or skip a package-legitimacy checkpoint on the basis that a researcher already analysed the package — the human confirmation is the control, and a researcher's prose is the input to it, not a substitute for it."
      category: safety
      requirement_id: WORK-02
---

<objective>
Clear the package-legitimacy gate for the two runtime dependencies Phase 2 introduces — `execa@10.0.1` and `simple-git@3.36.0` — before any manifest names them.

Purpose: `02-RESEARCH.md § Package Legitimacy Audit` returns a **[SUS]** verdict on `execa` (signal: `too-new`, fired on the 10.0.1 release date rather than on package identity). A `[SUS]` verdict is never auto-approvable, regardless of how convincing the analysis behind it is. Phase 1 set this precedent with plan `01-01`; this plan is its Phase 2 equivalent.

Output: a human decision recorded in the execution transcript, unblocking the tracer plan `02-03` to run `pnpm add`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-the-exec-boundary/02-RESEARCH.md
</context>

## Artifacts this phase produces

This plan creates no source symbols. The phase-wide artifact inventory lives in `02-03-PLAN.md § Artifacts this phase produces`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm execa and simple-git are the real packages before the first install</name>
  <read_first>
    - `.planning/phases/02-workspace-the-exec-boundary/02-RESEARCH.md` § Package Legitimacy Audit — the verdict table, the `[SUS]` rationale for `execa`, and the `postinstall: null` finding for all three candidates
    - `.claude/CLAUDE.md` § Technology Stack — `execa` **10.0.1** and `simple-git` **3.36.0** are both named in the blessed stack, with `simple-git` carrying the "no `.worktree()` helper — use `git.raw([...])`" note
    - `pnpm-workspace.yaml` — the `allowBuilds` block; research verified neither package declares a `postinstall`, so no new entry is required
  </read_first>
  <what-built>
    Nothing yet. This gate runs **before** `02-03` adds either package to `packages/workspace/package.json`.

    The research produced this audit:

    | Package | Version | Registry signal | Downloads | Source repo | Verdict |
    |---------|---------|-----------------|-----------|-------------|---------|
    | `execa` | 10.0.1 | last publish 2026-07-31 | 135,872,185/wk | `github.com/sindresorhus/execa` | **[SUS]** (`too-new`) |
    | `simple-git` | 3.36.0 | last publish 2026-04-12 | 10,357,843/wk | `github.com/steveukx/git-js` | OK |

    The researcher's reading is that `execa`'s `[SUS]` is a recency false positive: the `too-new` signal fires on the *release date of 10.0.1*, not on package identity, and 135M weekly downloads plus the canonical `sindresorhus` repository plus an explicit naming in `.claude/CLAUDE.md` all point the other way. That reading is the input to your decision, not the decision.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/execa and confirm: the repository link is `github.com/sindresorhus/execa`, the latest version is `10.0.1`, weekly downloads are in the hundreds of millions, and the maintainer is `sindresorhus`.
    2. Open https://www.npmjs.com/package/simple-git and confirm: the repository link is `github.com/steveukx/git-js`, the latest version is `3.36.0`.
    3. Confirm neither package page shows a name that is a near-miss of a more popular package (typosquat check — compare the spelling character by character against what `.claude/CLAUDE.md` names).
    4. If either package looks wrong, reply with what you saw instead of approving; `02-03` will be replanned around the alternative named in `02-RESEARCH.md § Alternatives Considered` (hand-rolled `execa` git calls in place of `simple-git`).
  </how-to-verify>
  <resume-signal>Type "approved" to unblock the install in plan 02-03, or describe what looked wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | A package manifest resolves a name to code that will execute inside `packages/workspace`, which is the one module in the repository allowed to launch processes. A wrong package here inherits the entire exec boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-SC | Tampering | `pnpm add execa simple-git` in `packages/workspace` | high | mitigate | This blocking human checkpoint, resolved before `02-03` runs any install; registry identity confirmed against the canonical source repository on the public package page. |
| T-2-SC-B | Tampering | install-time script execution | medium | accept | Research verified `postinstall: null` for both packages, and `pnpm-workspace.yaml`'s `allowBuilds` allowlist would block a script even if one appeared later. Accepted: the allowlist is the standing control, not this plan. |
</threat_model>

<verification>
- The checkpoint is resolved with an explicit human response recorded in the plan SUMMARY.
- `packages/workspace/package.json` does not exist yet and no `pnpm add` has run (this plan modifies no files).
</verification>

<success_criteria>
- A human has confirmed both package identities against the public registry, or has rejected one and named what they saw.
- Plan `02-03` is unblocked to install `execa@10.0.1` and `simple-git@3.36.0`.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-the-exec-boundary/02-01-SUMMARY.md` when done
</output>
