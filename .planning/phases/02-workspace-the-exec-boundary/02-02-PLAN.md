---
phase: 02-workspace-the-exec-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.js
  - test/lint/fixtures/spawn-direct-import.ts
  - test/lint/fixtures/spawn-require.ts
  - test/lint/fixtures/spawn-dynamic-import.ts
  - test/lint/fixtures/spawn-dynamic-execa.ts
  - test/lint/no-restricted-imports.test.ts
autonomous: true
requirements: [WORK-02]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A static `import` of `node:child_process`, `child_process`, `execa`, or `simple-git` anywhere outside `packages/workspace` fails `pnpm lint` at severity 2."
    - "For **every** banned specifier — `node:child_process`, `child_process`, `execa`, `simple-git` — both the `require()` form and the dynamic `import()` form fail `pnpm lint` at severity 2 outside `packages/workspace`. The ban is not bypassable by changing the import form, and not bypassable by picking a different banned specifier."
    - "The `no-restricted-imports` path list and the `no-restricted-syntax` selector list are both derived from one specifier constant, so the two enforcement layers cannot cover different sets of specifiers."
    - "`packages/workspace` is the only path exempted from the spawn ban; the exemption is expressed once, as an `ignores` entry, and is readable from the resolved config."
    - "After the spawn ban lands, the resolved ESLint config for `packages/core/src/verdict/verdict.ts` still restricts both `node:fs` and the `@adl/*` sibling group, and still carries the refine and superRefine selectors."
    - "A file under `packages/workspace/` importing `execa` resolves to zero `no-restricted-imports` errors, while the identical import outside that directory reports at severity 2."
    - "For every file the architecture rules govern, exactly one `no-restricted-imports` configuration resolves, and its restricted-path list is the union of every ban that file is subject to — asserted by reading the resolved config, not by reading the config source."
  artifacts:
    - path: "eslint.config.js"
      provides: "The no-direct-spawn boundary rule (WORK-02) composed as one complete rule object per files glob"
      contains: "no-direct-spawn"
    - path: "test/lint/fixtures/spawn-direct-import.ts"
      provides: "Deliberate-violation fixture for the static-import form of the spawn ban"
    - path: "test/lint/fixtures/spawn-require.ts"
      provides: "Deliberate-violation fixture for the require() form"
    - path: "test/lint/fixtures/spawn-dynamic-import.ts"
      provides: "Deliberate-violation fixture for the dynamic import() form over a builtin specifier"
    - path: "test/lint/fixtures/spawn-dynamic-execa.ts"
      provides: "Deliberate-violation fixture for the dynamic import() form over a non-builtin banned specifier — the case the hand-written selectors missed"
    - path: "test/lint/no-restricted-imports.test.ts"
      provides: "Fixture registration for the four new fixtures, the Pitfall-1 regression guard, the per-specifier selector-coverage assertion, and the exemption proof"
  key_links:
    - from: "eslint.config.js"
      to: "test/lint/fixtures/spawn-direct-import.ts"
      via: "the adl/no-direct-spawn-fixtures config entry applies the same rule object the real source is linted with"
      pattern: "no-direct-spawn-fixtures"
    - from: "test/lint/no-restricted-imports.test.ts"
      to: "eslint.config.js"
      via: "calculateConfigForFile reads the resolved options rather than the config source"
      pattern: "calculateConfigForFile"
  prohibitions:
    - statement: "MUST NOT extend the spawn-ban exemption beyond `packages/workspace`. A second exemption makes success criterion 2 false while the lint rule still appears to be enforcing it, which is worse than having no rule — the build stays green and the boundary is gone."
      category: safety
      requirement_id: WORK-02
---

<objective>
Land the no-direct-spawn boundary as a build property: a lint rule that fails CI when any module outside `packages/workspace` launches a process, in any of the three import forms, and that provably does not erase the rules Phase 1 already relies on.

**MVP user story** (from ROADMAP.md § Phase 2 Goal): **As a** contributor to ADL, **I want to** have the build reject a direct process spawn the moment I write one, **so that** the container backend in v2 is a registry entry rather than a repository-wide call-site sweep.

Purpose: success criterion 2 is a *build* property, not a review property. `02-RESEARCH.md § Pitfall 1` and `§ Pitfall 2` each describe a way this rule ships looking correct while being hollow — overlapping flat-config entries silently deleting `@adl/core`'s purity bans, and `no-restricted-imports` being blind to `require()` and dynamic `import()`. Both were reproduced locally against this repository's own ESLint 10.8.1.

Output: a rule composed as one complete object per `files` glob with both enforcement layers derived from a single specifier tuple, four deliberate-violation fixtures, and regression guards that fail if the Phase 1 bans stop resolving or if any banned specifier loses its syntax coverage.

This plan is deliberately independent of the `packages/workspace` package. It runs in Wave 1 alongside the legitimacy gate, before the module it protects exists — which is D-27's whole argument, applied to the exec boundary: the rule lands before any adapter exists to break it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-the-exec-boundary/02-CONTEXT.md
@.planning/phases/02-workspace-the-exec-boundary/02-RESEARCH.md
@.planning/phases/02-workspace-the-exec-boundary/02-PATTERNS.md
@eslint.config.js
@test/lint/no-restricted-imports.test.ts
</context>

## Artifacts this phase produces

This plan creates these symbols. The full phase-wide inventory is in `02-03-PLAN.md`.

| Symbol / path | Kind | File |
|---|---|---|
| `SPAWN_MESSAGE` | const | `eslint.config.js` |
| `FORBIDDEN_SPAWN_SPECIFIERS` | const, **also a named export** | `eslint.config.js` |
| `FORBIDDEN_SPAWN` | const (derived) | `eslint.config.js` |
| `SPAWN_SYNTAX` | const (derived) | `eslint.config.js` |
| `SPAWN_BAN_RULES` | const | `eslint.config.js` |
| `WORKSPACE_EXEMPTION` | const | `eslint.config.js` |
| `adl/no-direct-spawn` | ESLint flat-config entry name | `eslint.config.js` |
| `adl/no-direct-spawn-fixtures` | ESLint flat-config entry name | `eslint.config.js` |
| `test/lint/fixtures/spawn-direct-import.ts` | new file (deliberate-violation fixture) | — |
| `test/lint/fixtures/spawn-require.ts` | new file (deliberate-violation fixture) | — |
| `test/lint/fixtures/spawn-dynamic-import.ts` | new file (deliberate-violation fixture) | — |
| `test/lint/fixtures/spawn-dynamic-execa.ts` | new file (deliberate-violation fixture) | — |

<tasks>

<task type="auto">
  <name>Task 1: Compose the spawn ban as one complete rule object per glob</name>
  <reversibility rating="costly">The exemption surface is what success criterion 2 measures; widening it later needs an audit of every module added since, and every downstream phase (3, 4, 5) writes its process-launching code against this single exemption.</reversibility>
  <files>eslint.config.js</files>
  <read_first>
    - `eslint.config.js` — the entire file. Lines 15-20 are the rule-table docblock whose `Extended by` column already names "Phase 2's no-direct-spawn rule"; lines 44-61 are the `PURITY_MESSAGE` / `FORBIDDEN_CORE_BUILTINS` `.map()` shape to copy; lines 77-100 are `CORE_PURITY_RULES` and the docblock explaining why one rule object per glob is mandatory; lines 110-126 are `VERDICT_SCHEMA_RULES`, the `no-restricted-syntax` selector-array template; lines 136-157 are `baseConfigs` and its `ignores` list; lines 179-200 are `architectureConfigs`, the real-source + fixtures pairing
    - `.planning/phases/02-workspace-the-exec-boundary/02-RESEARCH.md` § Pitfall 1 (overlapping entries replace rather than merge — reproduced with this repo's eslint 10.8.1), § Pitfall 2 (the two verified selectors), and § Code Examples § "The lint boundary" (the composition, including the glob-discipline paragraph)
    - `.planning/phases/02-workspace-the-exec-boundary/02-PATTERNS.md` § `eslint.config.js` (MODIFIED) — the one-complete-object-per-glob discipline and the `adl/*-fixtures` pairing convention
    - `.claude/CLAUDE.md` § Technology Stack — `node:child_process` `fork()` is named as the manager→worker spawn primitive for Phase 3
  </read_first>
  <action>
    Extend `eslint.config.js` in place. Do not add a second lint mechanism and do not introduce a custom plugin rule — `02-RESEARCH.md § Alternatives Considered` records that `dependency-cruiser` and a custom rule were both evaluated and not adopted.

    Add five new module-level constants next to the existing ones:

    - `SPAWN_MESSAGE` — one shared message string, in the voice of the existing `PURITY_MESSAGE`. It must state: direct process launch is banned outside `packages/workspace` (WORK-02); every process ADL starts, including agent CLIs, goes through `Workspace.exec()`; and that the container backend in v2 is therefore a registry entry rather than a call-site sweep. Add a sentence naming the Phase 3 consequence explicitly: the manager→worker `fork()` seam also lands as a named export of `packages/workspace`, so the exemption count stays at one.
    - `FORBIDDEN_SPAWN_SPECIFIERS` — a frozen tuple naming exactly the four banned specifiers: `node:child_process`, `child_process`, `execa`, `simple-git`. **This is the single source both enforcement layers derive from, and it is the whole mechanism by which they cannot cover different sets.** Export it as a named export alongside the file's default config export so the Task 3 assertions can iterate it; ESLint loads only the default export, so a named export beside it changes nothing about how the config resolves. Both the prefixed and bare builtin spellings are listed as separate entries for the reason lines 47-51 already give. `execa` and `simple-git` are on the list because each spawns a process internally; `simple-git` in particular is what makes D-17 necessary — ADL's own git client routes through its own `Workspace` instance rather than earning a second exemption.
    - `FORBIDDEN_SPAWN` — `FORBIDDEN_SPAWN_SPECIFIERS` mapped with the same `.map((name) => ({ name, message: ... }))` shape as `FORBIDDEN_CORE_BUILTINS`. Do not restate the specifier list here; consume the tuple.
    - `SPAWN_SYNTAX` — `FORBIDDEN_SPAWN_SPECIFIERS` **flat-mapped** into two selector objects per specifier, each carrying `SPAWN_MESSAGE`: one selecting a `CallExpression` whose callee name is `require` and whose first argument value matches an anchored pattern for that one specifier, and one selecting an `ImportExpression` whose source value matches the same anchored pattern. Eight selectors from four specifiers. Take the selector *shapes* verbatim from `02-RESEARCH.md § Pitfall 2` — the attribute paths `callee.name`, `arguments.0.value`, and `source.value` are the parts verified working on this repository's eslint 10.8.1 — and change only the pattern each carries. Build that pattern by anchoring the specifier at both ends and escaping any regex metacharacter in it, so a specifier added to the tuple later cannot yield a selector matching more than it names. No optional-`node:`-prefix alternation is needed, because the tuple already carries both spellings as separate entries: one derivation rule, and no hand-maintained alternation to drift from the import list.

      **This derivation is the fix for a hole the first draft of this plan shipped, and the reason it must not be hand-written back.** With the selectors written by hand against `child_process` alone, `await import('execa')` in any module outside `packages/workspace` passed `no-restricted-imports` (blind to the dynamic form, per Pitfall 2) *and* passed `no-restricted-syntax` (the selector named the wrong specifier), while `pnpm lint` stayed green and success criterion 2 was false. `execa@10` is ESM-only, so reaching it through a dynamic `import()` is the idiomatic form rather than an exotic bypass. Two of the four banned specifiers were unreachable by the syntax layer. Deriving both lists from one tuple is what stops that recurring — the specifier list must appear exactly once in this file.
    - `WORKSPACE_EXEMPTION` — a single-element array holding the glob `packages/workspace/**/*.ts`, used as the `ignores` of the broad entry. Comment it with why the exemption covers the whole package rather than only `src/`: the package's own test suite must be able to stand up a temp git repository and to exercise the exec path, and success criterion 2's wording is "outside the workspace module". One exemption, expressed once.

    Then compose the rules, obeying the one-complete-object-per-glob discipline:

    - `SPAWN_BAN_RULES` — an object carrying `no-restricted-imports` at `error` with `paths: FORBIDDEN_SPAWN`, and `no-restricted-syntax` at `error` spreading `SPAWN_SYNTAX`. This is the object for every file that is neither `@adl/core` source nor exempt.
    - Merge into `CORE_PURITY_RULES`: its `no-restricted-imports` `paths` becomes the existing `FORBIDDEN_CORE_BUILTINS` plus the entries of `FORBIDDEN_SPAWN` that are not already present (`child_process` in both forms is already banned in core, so add `execa` and `simple-git` without duplicating names), `patterns` stays `FORBIDDEN_CORE_SIBLINGS`, and it gains a `no-restricted-syntax` at `error` spreading `SPAWN_SYNTAX`.
    - Merge into `VERDICT_SCHEMA_RULES`: its `no-restricted-syntax` becomes `SPAWN_SYNTAX` spread first, then the existing refine and superRefine selectors unchanged. This merge is mandatory, not stylistic — `packages/core/src/verdict/**/*.ts` is matched by both the core entry and the verdict entry, and whichever configures `no-restricted-syntax` last replaces the other entirely.

    Register the entries in `architectureConfigs`. Order matters and must be: the broad `adl/no-direct-spawn` entry first (`files: ['**/*.ts']`, `ignores: WORKSPACE_EXEMPTION` plus `packages/core/src/**/*.ts` plus `test/lint/fixtures/core-*.ts` plus `test/lint/fixtures/verdict-*.ts`, `rules: SPAWN_BAN_RULES`), then the existing `adl/core-purity`, `adl/core-purity-fixtures`, `adl/verdict-schema`, `adl/verdict-schema-fixtures` entries unchanged in relative order, then a new `adl/no-direct-spawn-fixtures` entry (`files: ['test/lint/fixtures/spawn-*.ts']`, `rules: SPAWN_BAN_RULES`).

    `ARCHITECTURE_RULE_IDS` needs no change — both `no-restricted-imports` and `no-restricted-syntax` are already listed, and the exhaustiveness test at the bottom of the lint suite asserts registered ids equal exercised ids.

    Finally, update the rule-table docblock at the top of the file: move "Phase 2's no-direct-spawn rule" out of the `Extended by` column of the `no-restricted-imports` row and add a row for the spawn ban naming WORK-02, success criterion 2, and the two pitfalls it is composed against. Follow the existing prose density — every rule in this file carries its own argument.
  </action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint` exits 0 (the real source tree contains no violation yet, and the fixtures are globally ignored by `baseConfigs`).
    - `grep -c 'no-direct-spawn' eslint.config.js` is at least 2 (the real-source entry and the fixtures entry).
    - `grep -c 'no-direct-spawn-fixtures' eslint.config.js` is 1.
    - `grep -v "^\s*[/*]" eslint.config.js | grep -c "packages/workspace/\*\*/\*.ts"` is 1 — the exemption is expressed exactly once. Comment lines are filtered first: the rationale prose this task requires legitimately names the glob, and a bare count over the unfiltered file would be invalidated by the very comment it asks for.
    - `grep -v "^\s*[/*]" eslint.config.js | grep -c "'simple-git'"` is 1 — the specifier appears as a string literal in exactly one place, the tuple, which is the mechanical form of "both enforcement layers derive from one list". Comments are filtered for the same reason as above: the tuple's rationale comment names `simple-git` deliberately.
    - `grep -c 'FORBIDDEN_SPAWN_SPECIFIERS' eslint.config.js` is at least 4 — the declaration, the named export, and the two derivations that consume it.
    - Behavior: adding a fifth specifier to `FORBIDDEN_SPAWN_SPECIFIERS` and changing nothing else produces both a new restricted path and two new selectors in the resolved config. Confirm this once during execution by adding a throwaway specifier, reading the resolved config, then removing it, and record the observation in the plan SUMMARY.
    - `pnpm vitest run --project root` exits 0 — in particular the pre-existing "registers every architecture rule at error, never warn" and "declares no architecture rule at anything other than error" tests still pass.
  </acceptance_criteria>
  <done>The spawn ban is registered, `pnpm lint` is green, and no pre-existing lint test has regressed.</done>
</task>

<task type="auto">
  <name>Task 2: Four deliberate-violation fixtures — one per import form, plus the cross-specifier case</name>
  <files>test/lint/fixtures/spawn-direct-import.ts, test/lint/fixtures/spawn-require.ts, test/lint/fixtures/spawn-dynamic-import.ts, test/lint/fixtures/spawn-dynamic-execa.ts, test/lint/no-restricted-imports.test.ts, eslint.config.js</files>
  <read_first>
    - `test/lint/fixtures/core-fs-import.ts` — the whole file; copy its four-line header form verbatim in shape (owning plan id, which rule it trips, the never-compiled disclaimer, the research citation) and its "otherwise valid, otherwise clean TypeScript" property
    - `test/lint/fixtures/verdict-refine.ts` — the analog for a `no-restricted-syntax` fixture
    - `test/lint/no-restricted-imports.test.ts` lines 34-64 (the `FixtureCase` interface and the `FIXTURES` table) and lines 129-149 (the negative control that requires each fixture to report **zero** errors once the architecture rules are removed)
    - `eslint.config.js` lines 136-173 — `baseConfigs`, including `tseslint.configs.recommended`, which is what the negative control lints with
  </read_first>
  <action>
    Create four fixtures under `test/lint/fixtures/`, each tripping exactly one cell of the form-by-specifier grid the ban covers, each owned by plan `02-02` in its header comment:

    - `spawn-direct-import.ts` — a static `import` of `node:child_process`, used by an exported function so nothing is unused. Trips `no-restricted-imports`.
    - `spawn-require.ts` — a `require` call for `child_process` (bare specifier, to prove both spellings are covered), its result used by an exported function. Trips `no-restricted-syntax` via the require selector derived for `child_process`.
    - `spawn-dynamic-import.ts` — an `await import('node:child_process')` inside an exported async function, its result used. Trips `no-restricted-syntax` via the ImportExpression selector derived for `node:child_process`.
    - `spawn-dynamic-execa.ts` — an `await import('execa')` inside an exported async function, its result used. Trips `no-restricted-syntax` via the ImportExpression selector derived for `execa`. **This is the fixture that watches the derivation rather than the rule.** With the selector list hand-written against the builtin only, this exact file linted clean while the rule table still claimed the ban was complete — and it is the idiomatic way to reach an ESM-only package from a CommonJS-shaped call site, so it is the bypass a contributor writes by accident rather than on purpose. The other three fixtures cannot detect that failure, because all three name the builtin.

    Then register all four in the `FIXTURES` table in `test/lint/no-restricted-imports.test.ts`, following the existing row shape: repository-relative `file`, the `ruleId` each trips, and a `mentions` substring the report must contain (use the banned specifier itself, matching how the existing rows use `node:fs` and `@adl/db`).

    Note on the `execa` fixture specifically: this plan runs in Wave 1, before plan `02-03` installs `execa` anywhere, and that is fine. Both rules are purely syntactic — neither `no-restricted-imports` nor `no-restricted-syntax` resolves the specifier — and the fixtures are inside the global `test/lint/fixtures/**` ignore and outside every package's `tsconfig` `include`, so nothing tries to resolve or compile them. Do not add `execa` to a manifest to make this fixture "work"; that would put the package outside the exemption and defeat the point.

    **The negative control is the hazard here, and it must be resolved empirically, not guessed.** `baseConfigs` spreads `tseslint.configs.recommended`, which carries rules that may report on a `require` call or a floating dynamic import independently of the architecture rules — and the negative-control test asserts every fixture is clean under `baseConfigs` alone. Run `pnpm vitest run --project root` after adding the fixtures and read which rule ids the control reports. For each base rule that fires *only because the fixture must contain that construct*, add a narrowly-scoped entry to `baseConfigs` turning that specific rule off for `files: ['test/lint/fixtures/spawn-*.ts']`, with a comment naming the rule and stating that the fixture exists to carry the construct the architecture rule bans. Do not turn a base rule off globally, do not use an inline disable comment that could also silence an architecture rule, and do not weaken the negative control itself — the control is the only thing proving the positive assertions measure the rules under test.
  </action>
  <verify>
    <automated>pnpm vitest run --project root</automated>
  </verify>
  <acceptance_criteria>
    - `test/lint/fixtures/spawn-direct-import.ts`, `test/lint/fixtures/spawn-require.ts`, `test/lint/fixtures/spawn-dynamic-import.ts`, and `test/lint/fixtures/spawn-dynamic-execa.ts` all exist.
    - `pnpm vitest run --project root` exits 0, including the "reports every fixture only because of the architecture rules" negative control and the "exercises every rule id the architecture config registers" exhaustiveness test.
    - The suite output contains a passing case named for each of the four new fixture paths.
    - Behavior: `spawn-dynamic-execa.ts` is reported by `no-restricted-syntax`, and the report's message names `execa`. A report naming only the generic ban message does not satisfy this criterion, because it would pass with a single catch-all selector that the `FORBIDDEN_SPAWN_SPECIFIERS` derivation is specifically not.
    - `pnpm lint` still exits 0 — the fixtures remain inside the global `test/lint/fixtures/**` ignore, so CI is not red by construction.
  </acceptance_criteria>
  <done>Each import form is watched failing against a committed fixture, the non-builtin dynamic-import bypass is watched failing too, and the negative control is still meaningful.</done>
</task>

<task type="auto">
  <name>Task 3: Regression guard — prove Phase 1's bans survived, every specifier is covered, and the exemption is real</name>
  <files>test/lint/no-restricted-imports.test.ts</files>
  <read_first>
    - `test/lint/no-restricted-imports.test.ts` lines 152-205 — the `calculateConfigForFile` severity test is the pattern to extend; note it currently asserts only that each rule is *registered at error*, never what it bans
    - `eslint.config.js` (post Task 1) — the composed `CORE_PURITY_RULES`, `VERDICT_SCHEMA_RULES`, and `SPAWN_BAN_RULES` objects
    - `.planning/phases/02-workspace-the-exec-boundary/02-RESEARCH.md` § Pitfall 1 § "Verification step for the plan" — the exact assertion this task exists to add
  </read_first>
  <action>
    Add three tests to `test/lint/no-restricted-imports.test.ts`, in a new `describe` block named for the spawn boundary.

    First, the Pitfall-1 regression guard. Resolve the config for `packages/core/src/verdict/verdict.ts` with `calculateConfigForFile`, then assert on the resolved *options*, not merely on severity: the `no-restricted-imports` options must contain a restricted path named `node:fs`, a restricted path named `execa`, and a `patterns` group whose group list includes `@adl/*`; the `no-restricted-syntax` options must contain a selector mentioning `refine` and a selector mentioning `require`. Write the assertions so a failure names which expectation was lost — the failure mode this guards against is silent, so the message is the whole value.

    Second, the per-specifier selector-coverage assertion — the guard against the hole described in Task 1. Import `FORBIDDEN_SPAWN_SPECIFIERS` from `eslint.config.js` and, for a non-exempt path, assert that the resolved `no-restricted-imports` options carry a restricted path for **every** entry of the tuple, and that the resolved `no-restricted-syntax` options carry **at least two** selectors naming each entry — one for the `require` form and one for the dynamic-import form. Drive both loops off the imported tuple rather than writing four cases by hand: a specifier added to the ban then gains its assertions automatically, and a specifier whose selectors are lost goes red without anyone remembering to add a case. Do the same against `packages/core/src/verdict/verdict.ts`, which is the file both merges have to survive.

    Third, the exemption proof. Resolve the config for `packages/workspace/src/exec/run.ts` (a path that does not exist yet — `calculateConfigForFile` resolves by path, not by file existence, so this test is valid in Wave 1 and stays valid once plan `02-03` creates the file) and assert its `no-restricted-imports` options contain no restricted path named `execa`. Then resolve the config for a non-exempt sibling path under `packages/db/src/` and assert the opposite. Two assertions on the same rule from two paths is what makes "exactly one exemption" checkable rather than asserted.

    Fourth, the single-configuration invariant. For each of `packages/core/src/verdict/verdict.ts`, `packages/core/src/stage/stage.ts`, `packages/db/src/index.ts`, and `packages/plugin-sdk/src/index.ts`, assert that `no-restricted-imports` resolves to exactly one options object (a single-element options array after the severity), and that `no-restricted-syntax` likewise resolves once. This encodes the flat-config replacement semantics the whole composition is built around, so a future entry that overlaps a glob fails here rather than in production.
  </action>
  <verify>
    <automated>pnpm vitest run --project root</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run --project root` exits 0.
    - `grep -c 'calculateConfigForFile' test/lint/no-restricted-imports.test.ts` is at least 4.
    - `grep -c 'FORBIDDEN_SPAWN_SPECIFIERS' test/lint/no-restricted-imports.test.ts` is at least 1 — the coverage assertion iterates the tuple rather than restating it.
    - Behavior: replacing the derived `SPAWN_SYNTAX` with the two hand-written `child_process`-only selectors makes the per-specifier coverage assertion fail, naming `execa` and `simple-git` as the specifiers with no selector. Confirm this manually once during execution, restore the derivation, and record the observation in the plan SUMMARY — this is the exact defect the guard exists to catch, so it must be watched failing.
    - Temporarily reordering the `architectureConfigs` entries so the broad spawn entry is registered last makes the regression-guard test fail — confirm this manually once during execution, then restore the correct order and record the observation in the plan SUMMARY. A guard nobody has watched fail is the exact shape of Pitfall 8 that this repository's lint suite exists to prevent.
    - `pnpm lint` exits 0 and `pnpm -r typecheck` exits 0.
  </acceptance_criteria>
  <done>The Phase 1 bans are asserted by resolved options rather than by severity alone, every banned specifier is proven covered in all three import forms, the single `packages/workspace` exemption is proven from both sides, and both guards have been watched failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor source → build | A new module added in any later phase can reach the OS process table directly, bypassing every control in `packages/workspace`. The lint rule is the only thing standing between a future contributor and that bypass. |
| lint configuration → enforcement | An overlapping flat-config entry can silently delete an existing rule, so the configuration itself is a place where enforcement can disappear without any signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Elevation of Privilege | `eslint.config.js` — a future module calls `spawn` directly, bypassing zero-inherit env, scratch HOME, privilege drop, and config neutralisation | high | mitigate | Task 1's `no-restricted-imports` ban over `node:child_process`, `child_process`, `execa`, `simple-git`, at severity 2, with a single `packages/workspace` exemption; Task 2's static-import fixture watches it fail. |
| T-2-02 | Elevation of Privilege | the ban is bypassed by changing the import *form* — `require(...)` or `await import(...)`, both verified invisible to `no-restricted-imports` | high | mitigate | Task 1's `no-restricted-syntax` selectors, **derived two-per-specifier from `FORBIDDEN_SPAWN_SPECIFIERS`** so every banned specifier is covered in both forms; Task 2's `spawn-require.ts` and `spawn-dynamic-import.ts` fixtures watch two of the eight fail. |
| T-2-02b | Elevation of Privilege | the ban is bypassed by changing the *specifier* — the syntax layer names `child_process` only, so `await import('execa')` or `await import('simple-git')` outside `packages/workspace` passes both layers and `pnpm lint` stays green. `execa@10` is ESM-only, so this is the idiomatic form, not an exotic one | high | mitigate | The single-tuple derivation makes the two lists structurally identical; Task 2's `spawn-dynamic-execa.ts` fixture watches the non-builtin case fail; Task 3's per-specifier coverage assertion iterates the tuple and has been watched failing against the hand-written selectors. |
| T-2-03 | Tampering | the new spawn entry's glob overlaps `packages/core/src/**`, silently erasing the purity ban and the `@adl/*` sibling ban that D-27 exists to enforce — reproduced locally, and every existing lint test still passed | high | mitigate | Task 1's merge-into-the-existing-object composition plus explicit `ignores`; Task 3's regression guard asserts the resolved *options* still contain `node:fs`, `execa`, and the `@adl/*` group, and has been watched failing. |
| T-2-04 | Repudiation | a rule ships at `warn`, so CI stays green and the boundary is decorative | medium | mitigate | The pre-existing "declares no architecture rule at anything other than error" test covers every entry in `architectureConfigs`, including the two added here. |
| T-2-05 | Elevation of Privilege | the exemption list grows to a second path in a later phase, making success criterion 2 false while the build stays green | medium | mitigate | The exemption is a single named constant used once; Task 3 asserts the boundary from both sides; recorded as a `must_haves.prohibitions` item so a future phase's verifier flags it. |
</threat_model>

<verification>
- `pnpm lint` exits 0.
- `pnpm -r typecheck` exits 0.
- `pnpm vitest run --project root` exits 0 with four new fixture cases and four new resolved-config assertions.
- The Pitfall-1 regression guard has been observed failing under a deliberately wrong entry order, then restored.
- The per-specifier coverage assertion has been observed failing under hand-written `child_process`-only selectors, then restored.
</verification>

<success_criteria>
- All three import forms of a direct process launch fail the build outside `packages/workspace`, for every banned specifier and not only for the builtin.
- The import-path list and the syntax-selector list are derived from one constant, so they cannot come to cover different specifiers.
- `packages/workspace` is the only exemption, and that is checkable from the resolved config rather than by reading the config source.
- Every rule Phase 1 registered still resolves with the same restricted paths and patterns it had before this plan.
</success_criteria>

<output>
Create `.planning/phases/02-workspace-the-exec-boundary/02-02-SUMMARY.md` when done
</output>
