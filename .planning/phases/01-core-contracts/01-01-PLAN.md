---
phase: 01-core-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SPEC-01, SPEC-02]
user_setup: []

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Every package installed in this phase that was NOT covered by 01-RESEARCH.md § Package Legitimacy Audit has been confirmed against its registry page by a human before the first install command runs."
    - "The parser selection for SPEC-01 and SPEC-02 (Assumptions A1, A2) is confirmed or replaced by an explicit human decision, not assumed by the executor."
  artifacts:
    - path: ".planning/phases/01-core-contracts/01-01-SUMMARY.md"
      provides: "Recorded legitimacy verdict and the exact resolved version for each gated package"
      contains: "ajv"
  key_links:
    - from: ".planning/phases/01-core-contracts/01-01-SUMMARY.md"
      to: ".planning/phases/01-core-contracts/01-02-PLAN.md"
      via: "the confirmed package/version list is what plan 01-02 Task 1 installs"
      pattern: "@cucumber/gherkin|mdast-util-from-markdown|ajv"
  prohibitions:
    - "ADL must never install a package into @adl/core whose legitimacy was assumed rather than checked — the parsers chosen here decide how every acceptance criterion in every adopter repository is enumerated, and a compromised or abandoned parser silently changes criterion counts rather than failing loudly."
---

<objective>
Confirm the legitimacy and the selection of the three package families that 01-RESEARCH.md introduced *during research* rather than inheriting from `.claude/CLAUDE.md`, before a single `pnpm add` runs.

Purpose: `@cucumber/gherkin`, `mdast-util-from-markdown`, and the JSON-Schema validator are `[ASSUMED]` *selections* (Assumptions Log A1, A2, and Sources § Tertiary). They are not in the project's pinned Technology Stack. They also decide how acceptance criteria are enumerated — and D-01 makes criterion numbering a one-way door, so a wrong parser is expensive in a way a wrong logger is not.

Output: a recorded human verdict plus the exact resolved version for each gated package, consumed verbatim by plan 01-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-contracts/01-RESEARCH.md
@.planning/phases/01-core-contracts/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm parser and validator package legitimacy before the first install</name>
  <read_first>
    - `.planning/phases/01-core-contracts/01-RESEARCH.md` § Package Legitimacy Audit — the audit table, the `[SUS] = too-new` false-positive analysis, and the explicit provenance note naming `@cucumber/gherkin`, `@cucumber/messages`, and `mdast-util-from-markdown` as research-selected rather than CLAUDE.md-mandated
    - `.planning/phases/01-core-contracts/01-RESEARCH.md` § Assumptions Log rows A1, A2, A6 — the three claims this checkpoint exists to convert into decisions
    - `.planning/phases/01-core-contracts/01-RESEARCH.md` § Sources § Tertiary — `ajv` is named as neither version-verified nor legitimacy-checked
    - `.planning/phases/01-core-contracts/01-CONTEXT.md` § D-26 — the "one dependency: Zod" framing that A2 contradicts
  </read_first>
  <what-built>
    Nothing yet. This is the gate that runs before plan 01-02 Task 1 executes any `pnpm add`.

    Automated preparation the executor performs BEFORE pausing (do all of this first, then present the results):
    1. Run `npm view @cucumber/gherkin version dist-tags repository.url` and the same for `@cucumber/messages`, `mdast-util-from-markdown`, and `ajv`. Record the resolved `latest` version and the repository URL for each.
    2. Run `npm view ajv version time.modified scripts.postinstall` and record the result — `ajv` was never audited, so its recency, its `postinstall` value, and its resolved version are all unknown at plan time.
    3. Run `node <gsd-tools> query package-legitimacy check --ecosystem npm --packages ajv` if the seam is available; record the verdict verbatim including any `too-new` reason string.
    4. Confirm that `@cucumber/gherkin@42.0.1`, `@cucumber/messages@34.2.1`, and `mdast-util-from-markdown@2.0.3` still resolve at the exact versions 01-RESEARCH.md verified.

    Do NOT install anything in this task.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@cucumber/gherkin — confirm the source repository is `github.com/cucumber/gherkin` (the Cucumber organisation) and that weekly downloads are in the millions. This is the reference Gherkin implementation for SPEC-02.
    2. Open https://www.npmjs.com/package/mdast-util-from-markdown — confirm the source repository is `github.com/syntax-tree/mdast-util-from-markdown` (the unified/syntax-tree organisation). Accepting it means `@adl/core` gains roughly 34 transitive micromark packages, so D-26's "one dependency: Zod" becomes "Zod plus two parser families". 01-RESEARCH.md § Alternatives Considered records the trade: `marked` and a hand-rolled scanner are the fallbacks, and both lose the byte offsets that make CORE-05's verbatim retention and D-01's `textHash` exact rather than approximate.
    3. Open https://www.npmjs.com/package/ajv — confirm maintainer, repository, and recent publish activity. This is the independent JSON-Schema validator for the CORE-04 equivalence contract test; it was never audited during research. If you prefer a different validator, name it now and it will be substituted in plan 01-02.
    4. Review the resolved versions the executor printed in step 1 above and confirm each is the version to pin.
    5. Decide Assumption A6: 01-RESEARCH.md § Pitfall 4 recommends that a Gherkin `Scenario Outline` becomes ONE criterion retaining its `Examples` table verbatim, and is NOT expanded into one criterion per example row. Expansion multiplies the `AC-n` count by the row count and couples criterion IDs to test data — and D-01 makes criterion numbering one-way. Confirm no-expansion, or state the alternative.
  </how-to-verify>
  <acceptance_criteria>
    - The SUMMARY records, for each of `@cucumber/gherkin`, `@cucumber/messages`, `mdast-util-from-markdown`, and the chosen JSON-Schema validator: the exact version to pin, the source repository URL, and the human's APPROVED or REPLACED verdict.
    - The SUMMARY records the A6 decision as either `scenario-outline: one-criterion-with-examples` or an explicitly stated alternative.
    - The SUMMARY records `ajv` (or its named replacement) with a resolved version string — the phase cannot proceed with the validator unresolved, because plan 01-04's JSON-Schema equivalence test has no other way to be independent of Zod.
    - No `pnpm add`, `npm install`, or lockfile write happened during this plan — `git status --porcelain` reports no `pnpm-lock.yaml` and no `node_modules` change.
  </acceptance_criteria>
  <resume-signal>Type "approved" to accept all four packages at the printed versions plus no-expansion for Scenario Outline, or name the specific package/version/decision to change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → ADL's dependency graph | Third-party code enters `@adl/core`, the package every later phase imports and the one published to third-party harness authors |
| research assumption → implementation | A `[ASSUMED]` selection becomes a one-way contract decision (D-01 criterion numbering) if it ships unexamined |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-SC | Tampering | npm installs into `@adl/core` / `@adl/db` | high | mitigate | Blocking human checkpoint before the first install for every package not covered by 01-RESEARCH.md § Package Legitimacy Audit; exact-version pinning recorded in the SUMMARY and consumed by plan 01-02 |
| T-1-SC2 | Tampering | `ajv` (or replacement) — never legitimacy-checked | high | mitigate | `npm view` metadata printed and human-confirmed against the registry page in this task before install |
| T-1-SC3 | Spoofing | Look-alike package names (the `biome` vs `@biomejs/biome` class of typosquat named in `.claude/CLAUDE.md`) | medium | mitigate | Human confirms the source repository URL matches the expected organisation for each package, not just the package name |
</threat_model>

<verification>
- `git status --porcelain` shows no dependency or lockfile changes produced by this plan.
- The SUMMARY contains a version string and a verdict for all four gated packages.
</verification>

<success_criteria>
Plan 01-02 can run `pnpm add` with every version already decided and every non-CLAUDE.md package already human-confirmed — no install in this phase is made on an unexamined assumption.
</success_criteria>

## Artifacts this phase produces

This plan produces **no source symbols**. It produces one document:

- `.planning/phases/01-core-contracts/01-01-SUMMARY.md` — the recorded package verdict table and the A6 (Scenario Outline) decision.

Every symbol in this phase is new; the repository is greenfield. The full new-symbol inventory is listed in plans 01-02 through 01-10 and consolidated in `SKELETON.md`.

<output>
Create `.planning/phases/01-core-contracts/01-01-SUMMARY.md` when done.
</output>
