---
phase: 04-first-agent-backend-live-transcripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agent-claude-code/package.json
  - packages/agent-claude-code/tsconfig.json
  - packages/agent-claude-code/tsconfig.test.json
  - packages/agent-claude-code/vitest.config.ts
  - packages/agent-claude-code/src/index.ts
  - packages/agent-claude-code/src/version.ts
  - packages/agent-claude-code/test/smoke.test.ts
  - packages/agent-claude-code/test/fixtures/CAPTURE.md
  - packages/agent-claude-code/test/fixtures/claude-version.txt
  - packages/agent-claude-code/test/fixtures/stream-json-develop.ndjson
  - packages/agent-claude-code/test/fixtures/stream-json-max-turns.ndjson
  - packages/agent-claude-code/test/fixtures/result-json.json
  - packages/agent-claude-code/test/helpers/fake-claude.mjs
  - packages/cli/package.json
  - pnpm-lock.yaml
  - test/lint/fixtures/spawn-agent-backend.ts
  - test/lint/no-restricted-imports.test.ts
autonomous: false
requirements: [BACK-01, BACK-05]
user_setup:
  - service: claude-code-cli
    why: "The agent backend this phase adapts. `Workspace.exec` resolves `claude` from the child's PATH; there is no fallback — a missing or wrongly-versioned binary is what success criterion 3's preflight exists to diagnose."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API keys. Required because `--bare` disables OAuth and system-keychain reads, so the key must be supplied explicitly through the exec env allowlist (D-09)."
    dashboard_config:
      - task: "Install the pinned CLI: `npm i -g @anthropic-ai/claude-code@2.1.237`, then confirm `claude --version` runs."
        location: "The host that runs the ADL manager/worker"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pnpm --filter @adl/agent-claude-code test` exits 0 against a real, installable package that is enrolled in `pnpm test` with no edit to the root `vitest.config.ts`."
    - "A source file under `packages/agent-claude-code/src/` that imports `execa` or `node:child_process` is reported by `adl/no-direct-spawn`, and the rule's exemption list still names exactly one package."
    - "The pinned Claude Code CLI version is a single exported constant, and the recorded fixtures were captured from a real invocation of that exact version rather than transcribed from documentation."
    - "A test can exercise the event translator against recorded `stream-json` output without shelling out to a real, billed CLI invocation."
  artifacts:
    - path: packages/agent-claude-code/package.json
      provides: "The @adl/agent-claude-code package manifest"
    - path: packages/agent-claude-code/src/version.ts
      provides: "PINNED_CLAUDE_CODE_VERSION"
    - path: packages/agent-claude-code/vitest.config.ts
      provides: "The agent-claude-code vitest project"
    - path: packages/agent-claude-code/test/fixtures/stream-json-develop.ndjson
      provides: "A real captured NDJSON event stream"
    - path: packages/agent-claude-code/test/fixtures/CAPTURE.md
      provides: "The exact argv, CLI version, and date each fixture was captured under"
    - path: packages/agent-claude-code/test/helpers/fake-claude.mjs
      provides: "A replay double that emits a recorded stream on stdout"
    - path: test/lint/fixtures/spawn-agent-backend.ts
      provides: "The deliberate-violation fixture proving the spawn ban reaches the new package"
  key_links:
    - from: "vitest.config.ts (root)"
      to: "packages/agent-claude-code/vitest.config.ts"
      via: "the `packages/*/vitest.config.ts` project glob — a new package enrols automatically"
      pattern: "packages/\\*/vitest.config.ts"
    - from: "eslint.config.js `adl/no-direct-spawn`"
      to: "packages/agent-claude-code/src/**"
      via: "the rule's `files: ['**/*']` glob, which the new package is NOT in the `ignores` list of"
      pattern: "WORKSPACE_EXEMPTION"
    - from: "packages/agent-claude-code/test/helpers/fake-claude.mjs"
      to: "packages/agent-claude-code/test/fixtures/stream-json-develop.ndjson"
      via: "the double reads the recorded fixture and writes it to stdout line by line"
---

<objective>
Stand up the Wave-0 infrastructure the rest of Phase 4 cannot be written without: the
`@adl/agent-claude-code` package, the one new CLI-side dependency, the assertion that the spawn ban
reaches the new package unchanged, and — the part that cannot be faked later — **recorded output
from a real invocation of the pinned Claude Code CLI**.

Purpose: `04-RESEARCH.md` § Pitfall 1 states that the exact four-flag combination this phase needs
(`--bare` + `--output-format stream-json` + `--verbose --include-partial-messages` +
`--append-system-prompt`) is not shown end to end in any official example, § Pitfall 2 states that
`claude --version`'s output shape is not a contracted API surface, and Assumption A1 flags the
`usage` object's field names as the weakest link in the whole research pass. All three are closed by
running the real thing once and writing down what came back.
Output: an installable, testable package; three recorded fixtures with their capture provenance; a
replay double so no later test has to pay for a real agent turn.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-first-agent-backend-live-transcripts/04-CONTEXT.md
@.planning/phases/04-first-agent-backend-live-transcripts/04-RESEARCH.md
@.planning/phases/04-first-agent-backend-live-transcripts/04-PATTERNS.md
@packages/manager/package.json
@packages/manager/tsconfig.json
@packages/manager/vitest.config.ts
@eslint.config.js
</context>

<phase_goal_note>
**MVP mode is on, but no user story exists for this phase.** The ROADMAP `**Goal:**` line for
Phase 4 reads: *"A real agent CLI, driven through the workspace, produces a commit and a transcript
the maintainer can watch as it happens."* That is an outcome statement, not an
`As a / I want to / so that` user story, so none has been invented here. Run `/gsd mvp-phase 4` if a
story is wanted; the plans proceed on the Goal as written. This matches the precedent set in
`03-01-PLAN.md`.
</phase_goal_note>

<edge_probe_ledger>
## Edge-probe accounting (phase-level ledger — 3 rows, 0 silent drops)

This phase has no `04-SPEC.md`, so `## Edge Coverage` was produced by the spec-less probe fallback.
The deterministic probe returned **3 applicable rows, all `unclassified` / `unresolved`** — the
classifier could not derive edge predicates from the three short requirement one-liners.

An `unclassified` row is **never** auto-resolved with a backstop marker. **3 == 0 authored as
`must_haves.truths` + 3 surfaced as flagged assumptions.** Nothing was dropped and nothing was
auto-dismissed.

| # | Requirement | Category | Disposition | Surfaced in |
|---|-------------|----------|-------------|-------------|
| 1 | BACK-01 | unclassified | **flagged assumption — unresolved** | `04-03` `<flagged_assumptions>` |
| 2 | BACK-05 | unclassified | **flagged assumption — unresolved** | `04-06` `<flagged_assumptions>` |
| 3 | OBS-02 | unclassified | **flagged assumption — unresolved** | `04-08` `<flagged_assumptions>` |
</edge_probe_ledger>

<prohibition_ledger>
## Prohibition ledger (§B recall — 5 kept, descriptor-less)

Stage 1 over-produced ~30 candidates across the three requirements; Stage 2 dropped the
routine-engineering ones (must-not-mutate-input, must-not-throw-on-a-malformed-line,
must-not-buffer-before-persisting — all owned by ordinary code review or by an explicit truth).
Five bespoke values/safety/transparency prohibitions survive, each authored **descriptor-less** (no
`check_*` scalar) into the `must_haves.prohibitions` block of its owning plan:

| # | Category | Owning plan | Substance |
|---|----------|-------------|-----------|
| P1 | safety | `04-06` | Never render a missing, mis-versioned, or non-functioning backend as a green stage outcome |
| P2 | transparency | `04-06` | Never attribute an agent-authored commit to a human |
| P3 | transparency | `04-09` | Never present a port shaped around one vendor's assumptions as vendor-neutral |
| P4 | transparency | `04-08` | Never present a filtered, truncated, or still-growing transcript as the agent's complete reasoning |
| P5 | transparency | `04-10` | Never record a computed cost as though the backend reported it, nor declare the spike closed on `unknown` |

**Canon-referral breadcrumbs (recalled, deliberately NOT minted):**
- The model API key reaching a transcript, a log line, or an error message is OWASP information-
  disclosure canon — covered by `/gsd-secure-phase`, by `env.ts`'s existing never-log discipline, and
  by `T-4-04` in `04-06`'s threat model; not minted here.
- Prompt injection through spec text instructing the agent to print its own environment is canon —
  covered by `/gsd-secure-phase` and `PITFALLS.md` Pitfall 11; not minted here.
- Path traversal through the `:id` param on the transcript route is canon — covered by `T-4-07` in
  `04-08`'s threat model; not minted here.
- Executing a repository's own `.claude/settings.json` hooks in a folder nobody trusted is arbitrary-
  code-execution canon — covered by `/gsd-secure-phase` and by `T-4-09` in `04-09`'s threat model;
  not minted here.
</prohibition_ledger>

<artifacts_produced>
## Artifacts this phase produces (plan 04-01 subset)

**New package:** `@adl/agent-claude-code`

**New files:**
- `packages/agent-claude-code/package.json`, `tsconfig.json`, `tsconfig.test.json`, `vitest.config.ts`
- `packages/agent-claude-code/src/index.ts` — the barrel
- `packages/agent-claude-code/src/version.ts` — exports `PINNED_CLAUDE_CODE_VERSION`
- `packages/agent-claude-code/test/smoke.test.ts`
- `packages/agent-claude-code/test/fixtures/CAPTURE.md` — capture provenance
- `packages/agent-claude-code/test/fixtures/claude-version.txt`
- `packages/agent-claude-code/test/fixtures/stream-json-develop.ndjson`
- `packages/agent-claude-code/test/fixtures/stream-json-max-turns.ndjson`
- `packages/agent-claude-code/test/fixtures/result-json.json`
- `packages/agent-claude-code/test/helpers/fake-claude.mjs` — exports the replay double's entry point
- `test/lint/fixtures/spawn-agent-backend.ts`

**New package manifest field:** `eventsource-parser@4.0.0` in `@adl/cli`'s `dependencies`

**New exported symbol:** `PINNED_CLAUDE_CODE_VERSION`
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — @anthropic-ai/claude-code and eventsource-parser</name>
  <read_first>
    - `.planning/phases/04-first-agent-backend-live-transcripts/04-RESEARCH.md` § "Package Legitimacy Audit"
    - `.planning/phases/03-manager-skeleton-state-leases-api-cli/03-01-PLAN.md` Task 1 (the shape this gate follows)
  </read_first>
  <what-built>
    Nothing yet. This gate runs BEFORE the first install of this phase, per the package legitimacy
    protocol. `04-RESEARCH.md`'s audit table returned `[SUS]` for both packages, in both cases from
    the `too-new` heuristic — which measures the most recent publish date rather than the package's
    age or its publisher's reputation, and therefore fires on every daily-release CLI from a major
    vendor. The research pass assessed both as false positives. The check is procedurally binding
    regardless of that assessment, and legitimacy checkpoints are never auto-approvable.
  </what-built>
  <how-to-verify>
    Open each page and confirm the facts listed under it.

    1. https://www.npmjs.com/package/@anthropic-ai/claude-code
       - The package is published under the `@anthropic-ai` scope, and that scope's other packages
         are Anthropic's own (`@anthropic-ai/sdk`, `@anthropic-ai/claude-agent-sdk`).
       - Version `2.1.237` is listed under `Versions` and is not deprecated.
       - Weekly downloads are in the millions, not the hundreds.
       - The newest version's publish date is recent — this is the `too-new` signal itself. Confirm
         the version history looks like a routine near-daily release cadence rather than a
         first-ever publish.
       - The audit recorded "no repository listed in npm metadata". Confirm that is still true and
         that it is not instead pointing at an unfamiliar third-party repository.

    2. https://www.npmjs.com/package/eventsource-parser
       - The `Repository` link points at `github.com/rexxars/eventsource-parser`.
       - Version `4.0.0` is listed and is not deprecated.
       - Weekly downloads are in the tens of millions.

    If either package looks wrong, say so instead of approving — Task 2 installs
    `eventsource-parser` and Task 3 runs the CLI binary, and neither should happen first.
  </how-to-verify>
  <acceptance_criteria>
    - A human has stated, in the resume signal, that `@anthropic-ai/claude-code@2.1.237` resolves under the `@anthropic-ai` scope and that `eventsource-parser@4.0.0` resolves to `github.com/rexxars/eventsource-parser`, and that neither version is deprecated.
    - No install of either package has run before this checkpoint resolves.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install and the real-CLI capture, or name the package and what looked wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold @adl/agent-claude-code, add eventsource-parser, and prove the spawn ban reaches the new package</name>
  <files>packages/agent-claude-code/package.json, packages/agent-claude-code/tsconfig.json, packages/agent-claude-code/tsconfig.test.json, packages/agent-claude-code/vitest.config.ts, packages/agent-claude-code/src/index.ts, packages/agent-claude-code/src/version.ts, packages/agent-claude-code/test/smoke.test.ts, packages/cli/package.json, pnpm-lock.yaml, test/lint/fixtures/spawn-agent-backend.ts, test/lint/no-restricted-imports.test.ts</files>
  <read_first>
    - `packages/manager/package.json` — the most recently created package manifest, and therefore the current template; note the five `scripts` keys and the `pretest`/`pretypecheck` build chain
    - `packages/manager/tsconfig.json` and `packages/manager/tsconfig.test.json` — copy shape, adjusting only the leading comment and the referenced projects
    - `packages/manager/vitest.config.ts` — copy, changing only the project `name`
    - `packages/workspace/src/index.ts` — the barrel header-comment style and the "why public" comment convention on each export group
    - `pnpm-workspace.yaml` — the `catalog:` entries for `typescript`, `zod`, `vitest`
    - `vitest.config.ts` (root) — confirms `packages/*/vitest.config.ts` auto-enrolment, so no root edit is needed
    - `eslint.config.js` — `WORKSPACE_EXEMPTION`, `SPAWN_BAN_RULES`, the `adl/no-direct-spawn` entry's `files`/`ignores`, the `adl/no-direct-spawn-fixtures` entry and the `test/lint/fixtures/spawn-*` glob it is registered against
    - `test/lint/no-restricted-imports.test.ts` — registration and assertion style, including how it asserts RESOLVED options for a real source path
    - `packages/cli/package.json` — the manifest `eventsource-parser` is added to
  </read_first>
  <action>
    Create `packages/agent-claude-code` as a real, installable package — no placeholders, per Phase 1
    D-25.

    **`packages/agent-claude-code/package.json`**: name `@adl/agent-claude-code`,
    `"type": "module"`, `version` `0.0.0`, `license` `MIT`, and the same `main`/`types`/`files`/
    `exports` shape `@adl/manager` has. `description` states its boundary: the Claude Code headless
    `AgentBackend` adapter — it translates and never re-drives the agent's own loop, and it reaches a
    subprocess only through `Workspace.exec`. `dependencies`: `@adl/core` at `workspace:*` and `zod`
    at `catalog:`. `devDependencies`: `@types/node` at `22.20.1`, `typescript` at `catalog:`,
    `vitest` at `catalog:`. It must list **neither** `execa` nor `@adl/workspace` — the adapter
    receives a `Workspace` instance as a parameter and never constructs one, which is what keeps the
    `adl/no-direct-spawn` exemption count at one. Five `scripts` keys matching the template;
    `pretypecheck` and `pretest` build `../core`.

    **`tsconfig.json` / `tsconfig.test.json`**: copy `@adl/manager`'s, adjusting `references` to
    `../core` only. Add a leading comment naming why `node` types are needed here: the adapter reads
    and writes nothing itself, but its tests read fixture files, so `node` types belong in
    `tsconfig.test.json` rather than in the source project — follow whichever split the manager's
    pair already uses and say which in the comment.

    **`vitest.config.ts`**: copy `@adl/manager`'s, changing only the project `name` to
    `agent-claude-code`. Do **not** edit the root `vitest.config.ts` — its own comment states that
    `packages/*/vitest.config.ts` enrols a new package automatically, which is what keeps concurrent
    plans off a shared file.

    **`src/version.ts`**: export `PINNED_CLAUDE_CODE_VERSION` as the exact string `2.1.237` (D-01 —
    pinned exactly, not a floor). Document in the docblock that D-01 chose an exact pin over a
    minimum-version floor because success criterion 4 requires the same feature on the same commit
    to receive the same prompt twice against a CLI whose own behaviour is not itself pinned, and
    that `04-RESEARCH.md` records a near-daily release cadence, so bumping this constant is a
    deliberate act that re-runs the Task 3 capture.

    **`src/index.ts`**: the barrel, in `packages/workspace/src/index.ts`'s header-comment style — a
    docblock stating what the package is for and what boundary it holds, and a "why public" comment
    above the export group. At this point it exports `PINNED_CLAUDE_CODE_VERSION` and nothing else;
    say so in the header and name the plans that add `run`, `probe`, and the event translator.

    **`test/smoke.test.ts`**: one test importing the package barrel and asserting
    `PINNED_CLAUDE_CODE_VERSION` is a three-part dotted version string. This is what makes the
    package testable rather than merely present.

    **`@adl/cli`**: add `eventsource-parser` at `4.0.0` to `dependencies` and run the install so
    `pnpm-lock.yaml` updates. Node has `fetch` but no `EventSource`, and this is the named stack
    choice for parsing the transcript stream on the client side; `04-08` is where it is used.

    **The spawn-ban assertion (`04-RESEARCH.md` § Pitfall 5).** The research pass asks the planner to
    *verify, not assume*, that adding this package does not require widening the exemption. Add
    `test/lint/fixtures/spawn-agent-backend.ts` — named to match the `test/lint/fixtures/spawn-*`
    glob the `adl/no-direct-spawn-fixtures` entry is registered against — containing a deliberate
    direct import of `execa`, with a header comment saying it exists to prove the rule has teeth for
    an agent-adapter package and must never be "fixed". Extend
    `test/lint/no-restricted-imports.test.ts` with two assertions: that linting the new fixture
    reports the `no-restricted-imports` violation for `execa`, and that the resolved
    `no-restricted-imports` options for the real path `packages/agent-claude-code/src/index.ts`
    still carry the spawn ban — i.e. the new package is not in the exemption list. Also assert the
    exemption list itself still has exactly one entry, so a future contributor adding a second one
    turns the suite red rather than passing review.

    Do not create `src/backend.ts`, `src/events.ts`, or `src/preflight.ts` here — `04-06`, `04-07`,
    and `04-09` own those, and an empty stub would be a file three later plans have to reconcile.
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm --filter @adl/agent-claude-code test &amp;&amp; pnpm lint &amp;&amp; pnpm vitest run --project root</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @adl/agent-claude-code test` exits 0 and executes `packages/agent-claude-code/test/smoke.test.ts`.
    - `pnpm test` includes a project named `agent-claude-code` with no edit to the root `vitest.config.ts` — assert by running `pnpm vitest run --project agent-claude-code` and getting a non-empty run.
    - `packages/agent-claude-code/package.json` lists `@adl/core` and `zod` in `dependencies`, and lists neither `execa` nor `@adl/workspace`.
    - `packages/cli/package.json` lists `eventsource-parser` at exactly `4.0.0`, and `pnpm-lock.yaml` resolves it.
    - A test lints `test/lint/fixtures/spawn-agent-backend.ts` and asserts at least one reported message names `execa`.
    - A test asserts the resolved `no-restricted-imports` options for `packages/agent-claude-code/src/index.ts` include the spawn ban, and that the spawn rule's exemption array has length 1.
    - `pnpm lint`, `pnpm -r typecheck`, and `pnpm format` all exit 0.
  </acceptance_criteria>
  <done>A real, enrolled, lint-covered adapter package exists, and the build says the spawn ban reaches it.</done>
</task>

<task type="auto">
  <name>Task 3: Capture what the pinned CLI actually emits — version string, event stream, turn-cap result</name>
  <precondition>`claude --version` runs on this host and prints 2.1.237, and ANTHROPIC_API_KEY is set in the environment this task runs in (user_setup step 1 complete).</precondition>
  <files>packages/agent-claude-code/test/fixtures/CAPTURE.md, packages/agent-claude-code/test/fixtures/claude-version.txt, packages/agent-claude-code/test/fixtures/stream-json-develop.ndjson, packages/agent-claude-code/test/fixtures/stream-json-max-turns.ndjson, packages/agent-claude-code/test/fixtures/result-json.json, packages/agent-claude-code/test/helpers/fake-claude.mjs</files>
  <read_first>
    - `.planning/phases/04-first-agent-backend-live-transcripts/04-RESEARCH.md` § "Common Pitfalls" Pitfall 1 and Pitfall 2, § "Assumptions Log" A1 and A3, § "Open Questions" 3, and § "Architecture Patterns" Pattern 1 (the exact argv) and Pattern 4 (the `usage_events` column mapping this capture is meant to confirm)
    - `packages/agent-claude-code/src/version.ts` — the pinned version this capture must be taken against
    - `packages/workspace/src/exec/env.ts` — the neutralisers and the reserved-variable rules, so the manual capture is run under an environment shaped like the one the adapter will actually get
  </read_first>
  <action>
    Run the real CLI three times against a throwaway git repository and record exactly what comes
    back. This is the task `04-RESEARCH.md` § Pitfall 1 asks for by name: *do not build the
    translator purely from the documented event shapes without one real capture.*

    Create the throwaway subject outside the ADL repository — a fresh `git init` directory under the
    OS temp dir with one committed file — so nothing captured here can touch this repository's
    working tree. Set `ANTHROPIC_API_KEY` explicitly in the child environment for every invocation;
    `--bare` disables OAuth and system-keychain reads, so an implicit credential will not be found.

    **Capture 1 — the version string.** Run `claude --version`, and record its complete stdout
    verbatim into `test/fixtures/claude-version.txt`, plus its exit code in `CAPTURE.md`. Pitfall 2
    records that this output shape is not a contracted API surface, so the fixture — not a regex
    written from memory — is what `04-07`'s parser is held to.

    **Capture 2 — the developer-shaped event stream.** Run the exact argv Pattern 1 specifies:
    `claude -p <instructions> --bare --output-format stream-json --verbose
    --include-partial-messages --append-system-prompt <system prompt> --permission-mode acceptEdits`,
    with instructions that ask for one small real edit and a commit inside the throwaway repository.
    Redirect stdout to `test/fixtures/stream-json-develop.ndjson` and keep every line, including
    partial-message deltas. Record stderr and the process exit code in `CAPTURE.md`.

    **Capture 3 — the turn-cap result.** Re-run the same argv with `--max-turns 1` added and
    instructions that cannot complete in one turn, into
    `test/fixtures/stream-json-max-turns.ndjson`. Open Question 3 asks whether a turn-cap hit
    surfaces as a distinct `result` subtype or looks identical to a normal completion; write the
    answer down in `CAPTURE.md` as an explicit finding, because it decides whether `04-06`'s
    translator can classify it as a `StageError` rather than as a legitimate outcome.

    **Capture 4 — the single-object result.** Run the same argv once more with
    `--output-format json` instead of `stream-json`, into `test/fixtures/result-json.json`. This is
    the payload Assumption A1 is about: record in `CAPTURE.md` the exact key path to
    `total_cost_usd` and the exact key names inside the usage object, and state plainly whether they
    are flat or nested under a per-model breakdown. `04-10` maps these onto `usage_events`' columns
    and cannot be correct if this is guessed.

    **Redact before committing.** Read every captured file and confirm no credential, absolute home
    path, or machine-identifying value survives; replace any that do with a clearly-marked
    placeholder and note the substitution in `CAPTURE.md`. A fixture is committed to a public
    repository and this is the one moment to catch that.

    **`test/fixtures/CAPTURE.md`** records provenance so a reader can tell a recorded fact from a
    remembered one: the CLI version reported by capture 1, the date, the OS and Node version, the
    full argv of each capture with the prompt text, each exit code, the Open Question 3 finding, the
    Assumption A1 field-name finding, and any redaction made. State explicitly that re-pinning
    `PINNED_CLAUDE_CODE_VERSION` obliges re-running this capture.

    **`test/helpers/fake-claude.mjs`** is the replay double the rest of the phase tests against: a
    small Node script that reads a fixture path from `argv`, writes its lines to stdout with a
    small delay between them so a consumer sees a genuinely incremental stream rather than one
    flush, and exits with a code taken from another argument. It performs no spawning and no network
    access. `04-RESEARCH.md` § Wave 0 Gaps names it: real-CLI tests are the checkpoint-gated
    exception, not the default, and every later test in this phase runs against this double unless
    it is explicitly a real-invocation proof.
  </action>
    Add a fixture-integrity test in `test/smoke.test.ts` (or a sibling `fixtures.test.ts`) that
    reads each captured NDJSON file, asserts it is non-empty, asserts every non-blank line parses as
    JSON, and asserts `claude-version.txt` contains the string held in `PINNED_CLAUDE_CODE_VERSION`.
    That last assertion is what makes a stale fixture a red build the day the pin moves.
  </action>
  <verify>
    <automated>pnpm --filter @adl/agent-claude-code test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/agent-claude-code/test/fixtures/claude-version.txt` exists and contains the value of `PINNED_CLAUDE_CODE_VERSION`, asserted by a test rather than by reading.
    - `packages/agent-claude-code/test/fixtures/stream-json-develop.ndjson` exists, is non-empty, and every non-blank line parses as JSON — asserted by a test that iterates the file.
    - `packages/agent-claude-code/test/fixtures/stream-json-max-turns.ndjson` exists and a test asserts it contains at least one line whose parsed object has a `type` of `result`.
    - `packages/agent-claude-code/test/fixtures/result-json.json` exists and parses as a single JSON object.
    - `CAPTURE.md` names the CLI version, the capture date, the full argv of each capture, each exit code, an explicit answer to Open Question 3 (does a turn-cap hit produce a distinguishable `result`?), and an explicit statement of the `usage` object's real key names and nesting (Assumption A1).
    - `packages/agent-claude-code/test/helpers/fake-claude.mjs` exists, takes a fixture path and an exit code as arguments, and a test drives it and receives the recorded lines back in order across more than one chunk.
    - `pnpm --filter @adl/agent-claude-code test` exits 0.
  </acceptance_criteria>
  <done>The three unknowns `04-RESEARCH.md` flagged — the version string's shape, the four-flag combination's real event stream, and the usage object's real field names — are recorded facts in the repository, and a replay double lets every later test use them without paying for an agent turn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → this repository's dependency graph | A package installed here runs with the developer's and later the daemon's privileges |
| the real Claude Code CLI → committed fixture files | Captured output is written into a repository that will be published; whatever the CLI echoed is echoed with it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-SC | Tampering | npm installs (`@anthropic-ai/claude-code`, `eventsource-parser`) | high | mitigate | Package-legitimacy gate plus the blocking human checkpoint in Task 1, resolved before any install runs; both packages were `[SUS]` on the `too-new` heuristic only, and the checkpoint is not auto-approvable |
| T-4-01 | Information Disclosure | committed capture fixtures | high | mitigate | Task 3's explicit redaction pass reads every captured file for credentials, absolute home paths, and machine-identifying values before commit, and records any substitution in `CAPTURE.md`. `ANTHROPIC_API_KEY` is supplied to the child but never echoed into a fixture |
| T-4-02 | Elevation of Privilege | the new adapter package reaching the process table directly | high | mitigate | `packages/agent-claude-code` is deliberately absent from `adl/no-direct-spawn`'s `ignores`, and Task 2 adds a deliberate-violation fixture plus an assertion that the exemption array still has exactly one entry, so widening it turns the build red |
| T-4-03 | Tampering | the throwaway capture repository | medium | mitigate | The capture subject is created under the OS temp directory outside this repository, so a real agent turn writing files cannot reach ADL's own working tree |
</threat_model>

<verification>
- `pnpm install` completes and `pnpm-lock.yaml` resolves `eventsource-parser@4.0.0`.
- `pnpm --filter @adl/agent-claude-code test` exits 0.
- `pnpm vitest run --project root` exits 0 with the new spawn-fixture assertions.
- `pnpm lint`, `pnpm -r typecheck`, and `pnpm format` exit 0.
- `pnpm test` runs a project named `agent-claude-code` with no edit to the root `vitest.config.ts`.
</verification>

<success_criteria>
- `@adl/agent-claude-code` is a real, installable, enrolled package with a pinned-version constant.
- The spawn ban demonstrably reaches the new package, and the exemption count is still one.
- Three recorded fixtures plus a provenance file replace the three research assumptions this phase would otherwise have been built on.
- A replay double exists, so no later test needs a real, billed agent turn to exercise the translator.
</success_criteria>

<output>
Create `.planning/phases/04-first-agent-backend-live-transcripts/04-01-SUMMARY.md` when done
</output>