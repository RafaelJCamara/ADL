---
phase: 04-first-agent-backend-live-transcripts
plan: 10
type: execute
wave: 5
depends_on: [04-07, 04-09]
files_modified:
  - packages/agent-claude-code/src/usage.ts
  - packages/agent-claude-code/src/backend.ts
  - packages/agent-claude-code/src/index.ts
  - packages/agent-claude-code/test/usage.test.ts
  - packages/manager/src/ipc/protocol.ts
  - packages/manager/src/worker-supervisor/supervisor.ts
  - packages/manager/src/worker-entry/stage-runner.ts
  - packages/manager/test/usage/recording.test.ts
  - .planning/STATE.md
autonomous: true
requirements: [BACK-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real agent invocation records exactly one usage row against its feature, round, and stage attempt, with the backend's own reported cost and a source of `reported` — never a figure ADL computed."
    - "All four token counters are populated when the backend reports them and left null when it does not — a backend that reports nothing stays distinguishable from one that reported zero."
    - "The worker never writes to the database: usage travels over the existing IPC channel and the manager performs the insert through the repository that already exists."
    - "A usage figure the backend did not report is recorded with a source of `unknown` and a null cost rather than a zero, and the run says so rather than appearing free."
    - "The cost-accounting spike STATE.md flags as blocking Phase 6 planning is closed by evidence from a real invocation, or STATE.md says plainly that it is not."
  prohibitions:
    - "MUST NOT record a cost ADL computed as though the backend reported it, and MUST NOT declare the cost-accounting spike closed while the recorded events carry an unknown cost source."
  artifacts:
    - path: packages/agent-claude-code/src/usage.ts
      provides: "usageFromResult — the backend's terminal event to a usage record"
    - path: packages/manager/test/usage/recording.test.ts
      provides: "The one-row, correct-join-keys, null-not-zero proofs"
    - path: .planning/STATE.md
      provides: "The blocker either closed with evidence or restated with what is still unknown"
  key_links:
    - from: "packages/manager/src/worker-entry/stage-runner.ts"
      to: "packages/manager/src/ipc/protocol.ts"
      via: "a usage message over the existing IPC channel — the worker reports, the manager writes"
    - from: "packages/manager/src/worker-supervisor/supervisor.ts"
      to: "packages/db/src/repository/usage.ts"
      via: "usageRepository(db).record — the existing writer, not a second insert path"
      pattern: "usageRepository"
---

<objective>
Record what an agent turn actually cost, and close the cost-accounting spike `.planning/STATE.md`
lists as blocking Phase 6 planning.

Purpose: D-06 makes this phase the place, because `adl dev-run` already produces the real agent turn
the spike needs and a separate throwaway script was explicitly not wanted. Phase 6's budget
enforcement will be designed against whatever this phase records, so recording it wrongly — a
computed figure labelled as reported, or four token counters collapsed into one — means Phase 6
designs against bad data and the spike is not actually closed however STATE.md is edited.
Output: a usage row per invocation with honest provenance, and a STATE.md entry that says either
"closed, here is the evidence" or "still open, here is what is unknown".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-first-agent-backend-live-transcripts/04-CONTEXT.md
@.planning/phases/04-first-agent-backend-live-transcripts/04-RESEARCH.md
@.planning/phases/04-first-agent-backend-live-transcripts/04-PATTERNS.md
@.planning/phases/04-first-agent-backend-live-transcripts/04-06-SUMMARY.md
@packages/db/src/repository/usage.ts
@packages/db/src/schema.ts
@packages/db/src/pricing.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (plan 04-10 subset)

**New file:** `packages/agent-claude-code/src/usage.ts`

**New exported symbols:**
- `usageFromResult(event)` → `AgentUsageRecord | undefined`
- `AgentUsageRecord` — model id, speed, four nullable token counters, nullable cost, and a cost source

**New IPC message kind:** `usage`, worker to manager, carrying the lease token and the usage record

**Updated:** `IPC_MESSAGE_KINDS` gains one entry; the supervisor gains a handler that performs the
insert through the existing usage repository

**Updated planning artifact:** `.planning/STATE.md` § Blockers/Concerns — the cost-accounting entry
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The backend's terminal event becomes a usage record with honest provenance</name>
  <files>packages/agent-claude-code/src/usage.ts, packages/agent-claude-code/src/backend.ts, packages/agent-claude-code/src/index.ts, packages/agent-claude-code/test/usage.test.ts</files>
  <read_first>
    - `packages/agent-claude-code/test/fixtures/result-json.json` and `CAPTURE.md` — the real terminal payload and, specifically, `CAPTURE.md`'s recorded answer to Assumption A1: the exact key path to the reported cost and the exact key names and nesting of the token counters. This mapping must be written against those recorded facts, not against the research table's provisional guesses
    - `packages/agent-claude-code/test/fixtures/stream-json-develop.ndjson` — the terminal event as it appears in the streaming form, which is the shape the adapter actually sees
    - `packages/db/src/schema.ts` — `UsageEventsTable` column by column, and the docblock stating why each token counter is nullable with no default and why an unpriceable model records no cost rather than zero
    - `packages/core/src/stage/agent.ts` — the usage and result event members as landed by `04-03`, and `AgentCapabilities`' declarations about whether the backend reports usage and cost
    - `packages/db/src/pricing.ts` and `packages/db/src/repository/usage.ts` — `priceAt` and `spendByCategory`'s unpriced-event counter, which is what consumes an honest null
    - `.planning/phases/04-first-agent-backend-live-transcripts/04-RESEARCH.md` § "Architecture Patterns" Pattern 4 (the column mapping table) and § "Common Pitfalls" Pitfall 4
    - `.planning/phases/01-core-contracts/01-CONTEXT.md` § D-14 and § D-31 — the overhead-versus-feature category split, and the rule that an unpriceable model records an unknown source rather than a fabricated zero
    - `./.claude/CLAUDE.md` § cost accounting — prefer the backend's reported cost over your own arithmetic, and never use a foreign tokenizer to estimate this vendor's tokens
  </read_first>
  <behavior>
    Write these as failing tests first.

    - `usageFromResult` applied to the recorded terminal payload returns a record whose cost equals the payload's own reported figure and whose source is `reported`.
    - It populates all four token counters from the payload, at the exact key path `CAPTURE.md` recorded.
    - A payload missing one token counter yields null for that counter and non-null for the others — never zero for the missing one.
    - A payload with no cost figure at all yields a null cost and a source of `unknown`, not a computed value and not zero.
    - A payload with no model identifier yields a record that still carries the token counts, with the model taken from the run's earlier started event rather than fabricated.
    - `usageFromResult` on an event that is not terminal returns undefined rather than a partially-filled record.
    - The record's cost source is never `computed` for this backend — the vendor reports a figure and the project rule is to prefer it over ADL's own arithmetic.
    - No tokenizer library is imported anywhere in the module, asserted by the package having no such dependency.
  </behavior>
  <action>
    Create `packages/agent-claude-code/src/usage.ts` exporting `usageFromResult(event)`.

    **Map against the recorded capture, not the research table.** `04-RESEARCH.md`'s Pattern 4
    mapping table is explicitly marked as an assumption for the token field names — Assumption A1
    names it as the weakest link in the entire research pass, and warns that a near-miss writes nulls
    into columns that should be populated while the spike appears closed. `04-01`'s `CAPTURE.md`
    holds the answer from a real invocation; read it and write the mapping from it. Cite the capture
    in a comment so a later reader can tell a recorded fact from a remembered one.

    **Four counters, nullable, never defaulted.** The schema's own docblock explains why: a backend
    that does not report cache tokens must stay distinguishable from one reporting zero, and that
    distinction cannot be recovered once destroyed at write time. Do not coalesce a missing value to
    zero for tidiness — every subsequent budget number inherits that lie, and `spendByCategory`'s
    unpriced-event counter exists precisely so an honest null can be reported as unpriced rather
    than silently folded into a total.

    **Cost source.** Use the backend's own reported figure and mark the source `reported`. Never
    `computed` for this backend: the project's stack rules say to prefer the backend's reported cost
    over ADL's arithmetic, and the reported figure being a client-side estimate that can differ from
    the bill is a known, documented property — recorded in the source field rather than corrected by
    guessing. When no figure is reported, record a null cost and a source of `unknown`, which is
    D-31's rule: an unpriceable event records no cost, never zero. Import no tokenizer; the stack
    rules ban estimating this vendor's tokens with a foreign tokenizer because it undercounts, and a
    budget gate built on an undercount is systematically wrong in the expensive direction.

    **Category.** A normal developer invocation is feature spend. Reserve the overhead category for
    the repair-reprompt path D-14 defines, which does not exist until a later phase — accept the
    category as a parameter so that phase does not have to reopen this function, and default it to
    feature spend.

    In `backend.ts`, surface the record on the run result so the caller does not re-parse the
    terminal event, and reconcile it with the capabilities the backend declares: if the backend
    declares it reports cost but the payload carried none, that is worth an event of the error kind
    in the transcript rather than a silent unknown — a capability that lies is more dangerous than
    one that says no. Export the surface from the barrel with a "why public" comment naming the
    worker as the consumer.
  </action>
  <verify>
    <automated>pnpm --filter @adl/agent-claude-code test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @adl/agent-claude-code test` exits 0 and executes `packages/agent-claude-code/test/usage.test.ts`.
    - A test reads `test/fixtures/result-json.json` from disk and asserts every one of the four token counters is non-null in the produced record — proving the mapping matches the real payload rather than a guess.
    - A test asserts the produced cost equals the figure in the fixture and the source is `reported`.
    - A test removes one token counter from a copy of the fixture and asserts that counter is null while the others remain non-null.
    - A test removes the cost figure and asserts a null cost with source `unknown`, and asserts the produced cost is not zero.
    - A test asserts the source is never `computed` across every fixture-derived case.
    - A test asserts `usageFromResult` returns undefined for a non-terminal event.
    - A test asserts the package manifest declares no tokenizer dependency.
    - A test asserts a backend declaring it reports cost, given a payload with none, produces an error-kind event.
    - `pnpm lint`, `pnpm -r typecheck`, and `pnpm format` exit 0.
  </acceptance_criteria>
  <done>The backend can say what its turn cost, in the schema's own vocabulary, with the provenance recorded rather than assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The worker reports, the manager writes — one row per invocation</name>
  <files>packages/manager/src/ipc/protocol.ts, packages/manager/src/worker-supervisor/supervisor.ts, packages/manager/src/worker-entry/stage-runner.ts, packages/manager/test/usage/recording.test.ts</files>
  <read_first>
    - `packages/manager/src/ipc/protocol.ts` — the two unions, the `.strictObject` + `.meta({ id })` convention, `IPC_MESSAGE_KINDS` and its compile-time exhaustiveness assertion, and the rule that every lease-scoped kind carries the lease token as a required field
    - `packages/manager/src/worker-supervisor/supervisor.ts` — how an inbound message is validated before any action is taken, how the lease token is checked against the current holder, and where a stale message is rejected on the fence
    - `packages/manager/src/worker-entry/stage-runner.ts` — where the run result and its usage record become available, and the module's ban on importing the database package
    - `packages/db/src/repository/usage.ts` — `usageRepository(db).record` and `NewUsageEvent`; this is the writer, and a second insert path is explicitly not wanted
    - `packages/manager/src/bookkeeping/attempt.ts` — the attempt address whose round id and stage attempt id become this row's join keys
    - `.planning/phases/04-first-agent-backend-live-transcripts/04-PATTERNS.md` § "Worker-entry may not import the database package" — the shared pattern this task follows
    - `.planning/phases/03-manager-skeleton-state-leases-api-cli/03-CONTEXT.md` § D-06 — the fencing rule the new message kind is subject to like every other lease-scoped kind
  </read_first>
  <behavior>
    Write these as failing tests first.

    - The usage message kind appears in the worker-to-manager union and in the frozen kind list; a kind added to one without the other fails the build.
    - The message carries the lease token as a required field, and a payload without it fails validation.
    - A usage message whose lease token is not the feature's current one is rejected without writing anything — the same fence every other lease-scoped write is subject to.
    - The supervisor, on a valid usage message, inserts exactly one row through the existing usage repository, carrying the feature id, the round id, and the stage attempt id from the assignment.
    - Two usage messages for the same attempt insert two rows — the manager does not deduplicate, because a repair reprompt legitimately produces a second event.
    - A usage message that does not validate reaches no repository method.
    - The worker entry's module graph still contains no database import.
    - An invocation whose backend reported no usage at all inserts no row rather than a row of nulls — an event that did not happen is not the same as one with nothing in it.
  </behavior>
  <action>
    **`src/ipc/protocol.ts`.** Add one worker-to-manager message kind carrying the lease token and
    the usage record, following the existing `.strictObject` + `.meta({ id })` convention and adding
    the discriminator to the frozen kind list — the compile-time exhaustiveness assertion will fail
    the build if it is missed, which is the point of that pattern. Make the lease token required, as
    every lease-scoped kind already is: D-06's fence applies in both places, and a usage write is a
    write.

    **`src/worker-entry/stage-runner.ts`.** Send the message when the run result carries a usage
    record, and send nothing when it does not. This module must not import the database package —
    the existing lint rule covers the whole directory and will say so — which is exactly why the
    report goes over the channel. Send it before reporting the stage result, so an invocation that
    is killed between the two still has its spend on the ledger; `.planning/STATE.md` records that
    burned spend surviving a crash is already a Phase 3 property, and this ordering preserves it.

    **`src/worker-supervisor/supervisor.ts`.** Validate first and drop anything that does not parse,
    following the existing handler's discipline of treating an unparseable message from a crashed or
    malicious worker as an infrastructure failure rather than trusted data. Check the lease token
    against the current holder and reject a stale one on the fence, exactly as the other lease-scoped
    kinds are. Then insert through `usageRepository(db).record` — never a second insert path, because
    that repository already holds the nullable-token and category discipline this table is subject to
    and a parallel writer is how the two drift. Populate the feature id, round id, and stage attempt
    id from the assignment the supervisor already holds rather than from the message, so a worker
    cannot attribute spend to a feature it does not hold the lease for.

    Do not deduplicate. A second usage event for one attempt is legitimate — D-14's repair reprompt
    is a second, separately-categorised event — and a manager that collapses them would silently
    under-report exactly the spend that phase exists to make visible.
  </action>
  <verify>
    <automated>pnpm --filter @adl/manager test &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @adl/manager test` exits 0 and executes `packages/manager/test/usage/recording.test.ts`.
    - A test asserts the frozen IPC kind list has exactly 8 entries and includes the usage kind.
    - A test asserts a usage payload without the lease token fails validation with a named reason.
    - A test sends a usage message carrying a superseded lease token and asserts no row was inserted.
    - A test drives a full dev-run against the replay double and asserts exactly one usage row exists for that feature, with round id and stage attempt id matching the attempt the run opened.
    - A test asserts the inserted row's feature id comes from the supervisor's assignment by sending a message naming a different feature and asserting the row still names the leased one.
    - A test sends two valid usage messages for one attempt and asserts two rows.
    - A test asserts an invocation whose backend reported no usage inserts no row.
    - `pnpm lint` exits 0, meaning the worker entry still imports no database package.
    - `pnpm -r typecheck` and `pnpm format` exit 0.
  </acceptance_criteria>
  <done>Every agent invocation's spend is on the ledger, written by the one process that writes, fenced like every other lease-scoped write.</done>
</task>

<task type="auto">
  <name>Task 3: Close the spike with evidence, or say plainly that it is still open</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` § "Blockers/Concerns" — the cost-accounting entry as written, and § "Session Continuity"
    - `.planning/ROADMAP.md` § "Phase 6: Accountant" — the prerequisite paragraph stating exactly what the spike must answer: reconcile reported cost against actual, and decide the degradation path when a backend's usage reporting is unreliable
    - `packages/agent-claude-code/test/fixtures/CAPTURE.md` — the recorded facts about what the backend reports
    - `packages/db/src/repository/usage.ts` — `spendByCategory`'s unpriced-event counter, which is the mechanism a degraded backend is reported through
    - `.planning/phases/04-first-agent-backend-live-transcripts/04-CONTEXT.md` § D-06 — what closing the spike was defined to mean
  </read_first>
  <action>
    Run one real `adl dev-run` against the real pinned CLI with a real credential, then read the
    recorded row back out of the database and write down what it says.

    Answer, in the summary and then in STATE.md, the three questions the ROADMAP's Phase 6
    prerequisite actually asks. **What did the backend report?** — the cost figure, the four token
    counters, and which of them were present. **How does the reported figure relate to the actual
    bill?** — compare it against the provider's own usage reporting for that invocation if it is
    visible, and if it is not yet visible, say so and say when it would be, rather than implying a
    reconciliation that did not happen. **What is the degradation path?** — state concretely what a
    backend that reports nothing produces: a null cost, an unknown source, and a nonzero unpriced-
    event count, which is what lets Phase 6 fall back to round and wall-clock caps and *say* it has
    rather than silently ceasing to enforce.

    Then edit `.planning/STATE.md`'s Blockers/Concerns section, honestly, in whichever direction the
    evidence points. If the row carries a reported cost, a reported source, and populated token
    counters, replace the blocker with a closed entry naming the date, the CLI version, the recorded
    figures, and where the evidence lives. If any of that is missing — most likely if the
    reconciliation against the actual bill could not be performed — leave the blocker in place and
    narrow it to exactly what is still unknown, so Phase 6's planning meets a smaller question rather
    than the same one.

    Prohibition P5 in this plan's `must_haves` is the rule here and it binds this task specifically:
    do not declare the spike closed while the recorded events carry an unknown cost source. Pitfall
    4 states the failure mode plainly — an unknown source on every invocation means the spike is not
    closed, whatever STATE.md says afterward — and the whole value of D-06 was getting a real answer
    at the moment the real call already had to happen.

    Edit STATE.md with a scoped replacement of the Blockers/Concerns entry. Do not rewrite the file.
  </action>
  <verify>
    <automated>pnpm test</automated>
    <human-check>
      Run one real `adl dev-run` with the pinned CLI and a real credential, then read the usage row
      back with a database query and confirm three things by eye: the cost source column reads
      `reported`; the cost figure is a plausible non-zero number for the turn you just watched; and
      the token columns that the capture said would be populated are populated. Then read the
      STATE.md entry and confirm it says what you just saw rather than what was hoped.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md`'s Blockers/Concerns section names the cost-accounting item either as closed with a date, a CLI version, the recorded figures, and a pointer to the evidence, or as still open with a narrowed statement of exactly what remains unknown.
    - The plan summary records the actual values read out of the usage row: the cost, the four token counters, and the cost source.
    - The plan summary states whether the reported figure was reconciled against the provider's actual billing and, if not, why not and when it could be.
    - The plan summary states the degradation path concretely in terms of the columns a caller would observe.
    - The STATE.md edit is a scoped replacement of the blocker entry; the rest of the file is unchanged, verifiable in the diff.
    - `pnpm test` exits 0.
  </acceptance_criteria>
  <done>Phase 6 can be planned against a recorded fact instead of an assumption, or it knows precisely which assumption it is still planning against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forked worker → the spend ledger | The worker reports figures the manager persists, and the worker runs alongside an agent |
| backend-reported figures → ADL's budget arithmetic | The cost figure originates in a vendor's client-side estimate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-38 | Tampering | a worker attributing spend to a feature it does not hold | high | mitigate | The usage message carries the lease token as a required field and is fenced against the current holder like every other lease-scoped write; the feature, round, and attempt ids on the inserted row come from the supervisor's own assignment rather than from the message. A test sends a message naming a different feature and asserts the row still names the leased one |
| T-4-39 | Repudiation | spend recorded as zero because it was not reported | high | mitigate | A missing figure records a null cost and an unknown source, never zero, and `spendByCategory` reports unpriced events as their own count rather than folding them into a total. Tests assert null-not-zero for each of the four counters and for the cost. Prohibition P5 |
| T-4-40 | Repudiation | a computed figure presented as the backend's own | high | mitigate | The source column distinguishes reported from computed, this backend only ever records reported or unknown, and a test asserts the computed value never appears across every fixture-derived case |
| T-4-41 | Denial of Service | an unparseable or flooding usage message from a crashed worker | medium | mitigate | Every inbound message is validated before any repository method is reached, following the supervisor's existing discipline; a message that does not parse is dropped and logged. Flooding is bounded by the fence — a superseded worker's messages are rejected without a write |
| T-4-42 | Information Disclosure | the model credential appearing in a recorded usage row | high | mitigate | The usage record carries only a model identifier, a speed tier, four counters, a cost, a source, and a category — no environment value and no free-text field into which a credential could travel. The schema's column list is the boundary |
</threat_model>

<verification>
- `pnpm --filter @adl/agent-claude-code test` and `pnpm --filter @adl/manager test` exit 0.
- `pnpm lint` exits 0 — the worker entry still cannot reach the database.
- `pnpm test` exits 0 across the workspace on both CI legs.
- The human check has been performed against a real invocation, or the summary and STATE.md both say plainly that it has not.
</verification>

<success_criteria>
- One usage row per invocation, joined to the right feature, round, and attempt, written by the manager.
- Reported cost recorded as reported; unreported cost recorded as unknown with a null, never a zero.
- All four token counters populated when reported and null when not.
- STATE.md's cost-accounting blocker is either closed with named evidence or narrowed to exactly what is still unknown.
</success_criteria>

<output>
Create `.planning/phases/04-first-agent-backend-live-transcripts/04-10-SUMMARY.md` when done
</output>
