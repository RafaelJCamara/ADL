{
  "name": "@adl/agent-claude-code",
  "version": "0.0.0",
  "type": "module",
  "description": "The Claude Code headless AgentBackend adapter. It translates the pinned Claude Code CLI's stream-json output into ADL's agent event shape and never re-drives the agent's own loop, and it reaches a subprocess only through the Workspace instance a caller passes in — it constructs no Workspace of its own.",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "pretypecheck": "tsc -b ../core",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "pretest": "tsc -b ../core",
    "test": "vitest run"
  },
  "dependencies": {
    "@adl/core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
